# 验收报告 · S079 打板P2仓位闸+龙虎榜

> Spec：`specs/S079-打板P2战法与仓位闸/spec.md`（瘦身版）
> 分支：`feature/S079-仓位闸龙虎榜`
> 验收日期：2026-08-18
> 验收人：Claude（orchestrator）

## 实现范围

三拆后 S079 瘦身版，只做**仓位闸扩展 + 龙虎榜黑名单层**（不依赖 S070 分时数据）。战法匹配拆 S081（backlog，等 S070 R7，已通知就绪）。

| 阶段 | 范围 | commit | 状态 |
|---|---|---|---|
| A 龙虎榜三分级 | R1-R5（取数/黑名单硬剔除/独食独大/散户霸榜/数据缺失） | 9ef2c7e | ✅ |
| B 仓位闸扩展 | R6-R8（_market_phase 4因子+红期硬熔断/cap_by_market_phase 叠加代数/STI 时序分离） | 9ef2c7e | ✅ |
| C 信号输出 | R9（pre_market_workflow 串两层后处理） | 433a61b | ✅ |
| D checklist 推送 | R10（routers/workflow 响应透传+前端 P2RiskPanel） | 1668dd0 | ✅ |
| E AC6 处置 | hot_money_seats 改用 em_get（复用防封底线） | 1668dd0 | ✅ |
| F 验收合集 | F1-F7 | 本报告 | ✅（F2/F6 待 live） |

## AC 逐条核对

| AC | 要求 | 验证 | 状态 |
|---|---|---|---|
| AC1 | _market_phase 扩展 4 因子 + 红期硬熔断 + 旧签名向后兼容 | B5 单测：`_market_phase(zt_count=40)` 旧签名返"普通"不报错；4 因子红期覆盖 | ✅ |
| AC2 | cap_by_market_phase 叠加代数 `min(weather_cap, market_phase_cap, max_total_position)` + 绿档不放宽 | B8/B9/B10 单测：叠加代数 + 绿档 cap=1.0 只收紧 + 互斥取 min | ✅ |
| AC3 | 龙虎榜三分级：黑名单>15%硬剔除/独食独大砍半/散户霸榜降权，复用 seat_engine | A4-A12 单测 37 个：硬剔除/砍半/降权全覆盖 | ✅ |
| AC4 | 龙虎榜数据"未取得"时硬剔除不可执行+警示+用户决策 | A11 单测：数据缺失不默认放行/拒绝，标"席位风控数据未取得" | ✅ |
| AC5 | 仓位参数+风控标记+checklist 推送，不接券商不下单，标注"参考值，非执行指令" | D1-D9 + C6 PARAM_DISCLAIMER；F4 确认无券商 API | ✅ |
| AC6 | hot_money_seats "绕过 em_get" 处置 | E1 核实 datacenter 与 push2ex 同源→E3 改用 em_get；E4 单测 7 个无裸 urlopen | ✅（联网实测待 live） |
| AC7 | 研判/买卖时机/仓位参数挂轻量风险提醒 | F3 自查：PARAM_DISCLAIMER + RISK_DISCLAIMER + checklist 风险提醒全标注 | ✅ |
| AC8 | PRD 阈值标注"探索性" + 验算口径 | config/seat_blacklist.yaml 标注探索性；**F2 financial_rigor.py 验算待 live**（需 60 日历史数据） | ⏳ 待 live |
| AC9 | S002 AC10 放宽到 §1.1 弱合规（spec §2.3 显式豁免） | spec §2.3 已豁免；F4 确认 S079 代码无战法匹配扩展+无新增止损/止盈（PositionAdvisor 既有 stop_loss/take_profit 是 S06x 先例，S079 未碰） | ✅ |
| AC10 | 不涉及战法匹配（S081）+ 不涉及参考价位（S081） | F4 代码审查：dragon_tiger_seat_filter/pre_market_workflow P2 部分无 match_strategies/broken_duration/止损止盈新增 | ✅ |

## 测试验证

| 测试套件 | 用例数 | 结果 |
|---|---|---|
| test_s079_dragon_tiger_seat_filter.py（A4-A12） | 37 | ✅ passed |
| test_s079_position_advisor_cap.py（B5/B12 + R6.3/R7.2/R7.3） | 37 | ✅ passed |
| test_s079_pre_market_workflow_p2.py（C1-C7） | 20+1 skip | ✅ passed（C8 集成占位 skip） |
| test_s079_workflow_p2_api.py（D1-D9 + E4） | 15 | ✅ passed |
| **S079 全套合计** | **109+1** | ✅ passed |
| 回归：hot_money_seats + workflow_snapshot + workflow_state | 64+40 | ✅ 无破坏 |
| 前端 tsc --noEmit | — | ✅ 无 error |

## 待 live 验收项

- **F2 AC8 阈值复算**：跑 `financial_rigor.py --thresholds prd_p2 --window 60d` 验证阈值判定逻辑 + 触发频率/空池率统计。需 60 日历史数据 + 联网取数。
- **F6 手动验收**：取近 5 个交易日真实数据跑全链路，输出仓位参数 + 风控标记 + checklist + 飞书推送格式确认。需盘中/盘后真实数据。
- **AC6 联网实测**：datacenter 限流阈值需 `pytest -m live` 验证（当前标注"可能同源"未实测）。

## 合规自查

- [x] §1.1 弱合规：研判/买卖时机/仓位参数属系统能力，挂轻量风险提醒
- [x] S002 参考价位隔离决议 + AC10 显式豁免（spec §2.3）
- [x] 不接券商不下单（AC7 工程底线）
- [x] 输出参数标注"参考值，非执行指令"（PARAM_DISCLAIMER）
- [x] PRD 阈值标注"探索性"，进 config 可配
- [x] AC7 防封：hot_money_seats 改用 em_get，datacenter 与 push2ex 共享 breaker
- [ ] 判断可复现：financial_rigor.py 验算待 live（F2）

## 关键设计决策

1. **扩展现有 _market_phase() 不引入新概念**（Q8=C 决议）：默认参数向后兼容，红期硬熔断作为覆盖层
2. **cap_by_market_phase 叠加代数**（Oracle B4 修复）：`min(weather_cap, market_phase_cap, max_total_position)`，绿档只收紧不放宽
3. **龙虎榜复用 seat_engine/hot_money_seats**（Oracle B2 修复）：不新增 akshare，PRD 黑名单三分级在既有通道上加层
4. **pre_market_workflow 串两层不破坏既有链路**（Oracle B1 修复）：advise_batch 后加 _apply_p2_post_filters，不改 advise/advise_batch 签名
5. **S002 隔离决议显式豁免**（Oracle B5 修复）：spec §2.3 引用 §1.1 + PositionAdvisor 既有 entry_price_range 先例

## 结论

S079 瘦身版实现完成，AC1-AC10 除 AC8（待 live 验算）外全部通过。可合并到 develop（F2/F6 待 live 不阻断合并，作为后续 live 验收任务）。
