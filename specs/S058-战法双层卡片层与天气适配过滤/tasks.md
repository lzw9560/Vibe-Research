# S058 任务拆分（原子任务 + 依赖 + 验收）

> develop 直提，勤 commit。

## R1 结构化层

| # | 任务 | 依赖 | 验收 |
|---|------|------|------|
| R1.1 | STRATEGY_REGISTRY 每战法补 `weather_regimes` + `aliases` 字段（初始映射：连板接力=[晴天]、首板挖掘=[阴天]、反包=[极端反弹]、低吸龙头=[晴天,阴天]、其余标注待校准） | — | schema 单测（每战法字段存在） |
| R1.2 | 适配度纯函数：weather_state × regimes → 适配/不适配/中性（未知天气或 regimes 空 → 中性） | R1.1 | 三态逻辑单测 |

## R2 卡片层

| # | 任务 | 依赖 | 验收 |
|---|------|------|------|
| R2.1 | `strategies/cards/` 目录 + 卡片模板约定（适用天气/核心逻辑/入场条件/条件单思路/风险点/退出参数 + 尾部风险提醒） | — | 模板文档化 |
| R2.2 | 现有 6 战法卡片撰写（内容取自注册表参数 + 现有策略逻辑，不虚构胜率） | R2.1 | 卡片与 code 一一对应断言测试 |
| R2.3 | CI 一致性断言：cards/ 文件名集合 == 注册表 code 集合 | R2.2 | 测试 |

## R3 AI 工具

| # | 任务 | 依赖 | 验收 |
|---|------|------|------|
| R3.1 | `query_strategy_card(code)` @register_tool 注册（读卡片返文本；code 缺失返 error dict；aliases 检索） | R2.2 | 工具单测（命中/缺失/别名） |
| R3.2 | chat + MCP 双出口冒烟（手动：问「连板接力战法」返回卡片文本） | R3.1 | 冒烟记录 |

## R4 软过滤接线

| # | 任务 | 依赖 | 验收 |
|---|------|------|------|
| R4.1 | 简报战法命中带适配度字段（routers/workflow） | R1.2 | 端点测试 |
| R4.2 | 不适配命中降权排序（后移，不屏蔽） | R4.1 | 排序单测 |
| R4.3 | 前端：战法展开区适配度标签 + 卡片 Markdown 渲染 | R4.1 | vitest + tsc + 走查 |

## 执行序

R1.1 → R1.2；R2.1 → R2.2 → R2.3；两线并行 → R3.1 → R3.2；R4.1 → R4.2/R4.3。
