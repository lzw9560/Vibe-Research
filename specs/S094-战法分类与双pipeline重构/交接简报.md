# S094 交接简报（opencode 会话 → Claude 会话）

> 写于 2026-08-23，本会话（opencode）收工，S094 实施移交给 Claude。
> 接手前先读本文 + `specs/S094-战法分类与双pipeline重构/{spec,plan,task}.md`。

## 1. 当前状态

- **分支**：`feature/S094-战法分类与双pipeline重构`（直接在此分支继续，勿新开分支）
- **S0 硬门**：✅ 已闭合（S095 gene_scores 写路径守卫，代码已同时落地本分支和 develop）
- **S1 因子层**：✅ 已完成（commit `c944360`，task T1-T6 + G1 已勾选），137 策略相关测试通过

## 2. 下一步：从 S2 开始

按 `plan.md` / `task.md` 的 S2→S6 顺序：
- **S2** 候选生产拆分 + sector_rank（T7-T9）
- **S3** StrategyContext 扩展 + match 分流 + confidence 复用 + 接线（T10-T17，含 R26/R28）
- **S4** 5 根因修复（T18/T19/T19a/T21）
- **S5** 前端双 pipeline 分区（T23-T26）
- **S6** 全量回归 + playwright（T27-T29）

## 3. 🔴 不可回退的拍板项（Oracle 两轮审查 + 用户拍板）

1. **R4 不委托** `compute_strategy_score`——volume_signal 下沉 match 层，候选用单一 PatternScan factors dict
2. **R10 只 3 战法**改读 PatternScan（pattern_reversal/low_absorption/platform_breakout）——**reverse_package 不在 R10**，保留 db_based.py 炸板池
3. **funnel_type 必填**（无默认值，防 None=全跑 crash）
4. 放量阈值：`compute_ma5_slope>0` / `shadow_length_pct>=4` / `volume_breakout_ratio>=1.2`（今量/前5日均量口径，沿用 1.2 保守值）
5. R9 dragon_head 条件化波及 6 个消费方（方向正确，已在 spec 显式声明）
6. zt_real 走 R16 变体（_emotion 加字段 + sti_timeline 持久化），**不走已删的 R15**
7. R19 已砍（sector_divergence 前端弃用）；R21 已并入 R1

## 4. 已知债务 / 观察项

- **全量回归待补**：S1 提交时跳过了耗时全量测试（外部网络慢测试导致超时）——Claude 接手后第一步先补跑：`cd backend && ../.venv/bin/python -m pytest tests/ -m "not live" --no-cov -q`，基线 **2226 passed**（唯一允许失败：S066 pre-existing）。若 S1 引入回归，先修再继续
- **盘前回退假设**（S095 spec §8 盲点 3）：盘前请求当日时东财可能静默回退，复现再议
- **test_s081_prd_strategies.py 已被 fix-21 调整**——若与 PRD 预期冲突，查提交 `c944360` 的 diff 确认意图

## 5. 并发纪律（AGENTS.md，硬约束）

- **不直接动 develop**——S094 是 large 级，收尾 `git merge --squash` 进 develop，**合并后立即删分支**
- **不与其他会话同时跑全量测试**（已撞过 SQLite 锁）
- 勤提交、小步提交，不留长生命未提交工作树
- 每阶段末更新 `task.md` 勾选 + 阶段性报告给用户

## 6. 关键文件索引

| 内容 | 路径 |
|---|---|
| spec（权威） | `specs/S094-战法分类与双pipeline重构/spec.md` |
| plan/task | 同目录 |
| S095 spec（已完成，参考守卫范式） | `specs/S095-gene_scores写路径修复与日期守卫/spec.md` |
| S095 守卫实现 | `backend/limitup_screener/service.py`（`_assert_not_future_date`，勿破坏） |
| S1 成果 | `backend/strategies/pattern_scan.py`（_compute_ma/compute_shadow_length_pct/compute_ma5_slope） |
| 权重源 | `.vibe-research/strategy_weights.json`（non_limitup 权重） |
