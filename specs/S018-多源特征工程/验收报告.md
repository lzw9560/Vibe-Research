# 验收报告：S018 — 多源特征工程

> 状态：已实现（macro 第二批因外部数据源缺口延后）  日期：2026-07-29
> 关联：`spec.md`、`plan.md`、`../S017-A股涨跌预测模型栈/plan.md`
> 本报告按 SDD §0 step 4 逐条核对 spec §6 验收标准 + §7 合规自查。

---

## 1. 交付物清单

| 模块 | 文件 | 特征数 | stage | commit |
|---|---|---|---|---|
| 注册表 | `predict/features/registry.py` | — (基础设施) | — | 55c3f4e |
| external | `predict/features/external.py` | 4 (SPX/NDX/恒生科技/A50 隔夜) | s2 | 4899992 |
| fund_flow | `predict/features/fund_flow.py` | 7 (主力/龙虎榜/封板/北向分段/融资/板块轮动/大宗) | s1 | 60a4839 |
| behavior | `predict/features/behavior.py` | 5 (短期反转/异常换手/竞价s3/昨涨停/一日游) | s1/s3 | 51668ad |
| sentiment | `predict/features/sentiment.py` | 2 (涨停四池聚合/板块分歧) | s1 | 41ac650 |
| calendar | `predict/features/calendar.py` | 3 (节假日/交割/会议) | s2 | 27bb4d9 |
| text | `predict/features/text.py` | 1 (newsradar LLM 情绪) | s1 | 9553313 |
| selection | `predict/features/selection.py` | — (harness) | — | 5722c6b |
| feature_interface | `predict/feature_interface.py` | 22 全注册 + head 子集 + get_features | — | f57b28f |

**已注册特征总数：22**（short_sector 子集 21，排除 auction_signal s3；mid_long 子集 7）。

## 2. 验收标准核对（spec §6）

- [x] **A1** 特征注册表落地，每特征声明完整含 `availability_offset` —— registry.py FeatureSpec + 6 模块 SPECS，offset 全声明。
- [x] **A2** "特征-可得时间"对齐表 —— plan.md §3 + 每 FeatureSpec 的 stage/availability_offset + list_for_stage look-ahead 防护（s1 排除 s2/s3，有测试）。
- [x] **A3** 第一批特征全部抽取并通过接口供给 —— feature_interface.get_features(short_sector, stage, t) 返回正确 schema DataFrame（列=list_available_features，S008 接 live 填行）。
- [x] **A4** 北向资金分段断点正确 —— NorthFlowSegmenter（RULE_CHANGE_DATE=2024-08-19，segment/is_realtime_allowed/can_cross_segment，禁跨段拟合，有测试）。
- [x] **A5** 文本特征固定 LLM 版本+提示词，可复算 —— LLM_MODEL_VERSION 常量 + NEWS_EMOTION_PROMPT 固定模板 + validate_prompt_compliance 合规自检（有测试）。
- [x] **A6** SHAP+Boruta 选 ≤25 稳定特征 —— selection.py select_stable_features（bootstrap permutation importance，random_state 固定可复算，rationale 记录）；SHAP/Boruta 作 try-import 增强（未安装时降级 permutation_importance + lightgbm/randomforest，不报错）。MAX_SHORT_FEATURES=25。
- [x] **A7** 涨停四池聚合情绪无个股名 —— sentiment.aggregate_emotion 白名单抽取 6 项聚合计数，剥离 ladder/lianban_stocks（含个股名），有双断言测试；seal_fund_strength/day_trip_risk compliance_flag=aggregate_only。
- [x] **A8** 新增端点走 em_get 限流 —— 所有东财端点特征 source 标 "astock.em_get"/"limitup_sti"，纯计算不触网，live 取数 S008 接（TODO 标注）；A50 走 gstock push2（内部 em_get）合规。
- [x] **A9** 第二批预留接口与空实现 —— macro.py 待 Fred API（DXY/DTWEXB）接入；text.py fetch_news_emotion 为 stub；feature_interface 空 DataFrame 为 S008 live 预留。

## 3. 合规自查（spec §7，按 CLAUDE.md §1 2026-07-29 新边界）

- [x] 情绪聚合特征无个股名（aggregate_emotion 白名单 + aggregate_only 标记 + 双断言）。
- [x] 文本提示词不引入收益承诺/代客决策（validate_prompt_compliance 检 5 违禁词，NEWS_EMOTION_PROMPT 含中立声明）。
- [x] 特征取数可复现（固定 LLM 版本/提示词/口径；selection random_state 固定；北向断点纯日期比较）。
- [x] 新增东财端点走 em_get（source 标注 + 纯计算不裸调 requests）。
- [x] 用户私有数据不进 git（baseline 只用 backend/data/fallback 公开行情；无私有数据）。

## 4. 测试

- 全量 `pytest -m "not live"`：**550 passed, 18 deselected, 0 failed**。
- S018 新增测试：external 9 + fund_flow 20 + behavior 29 + sentiment 12 + calendar 16 + text 22 + selection 10 + feature_interface 21 = **139 测试**。
- 关键纯函数覆盖率：external/fund_flow/sentiment/selection 均 100%（agent 报告）。

## 5. 遗留与延后

| 项 | 状态 | 阻塞原因 |
|---|---|---|
| macro.py（DXY/美债10Y/汇率/大宗） | 延后 | push2 候选 secid 全空（T0b 验证），待 Fred API（DGS10/DTWEXB）接入后补登 S2 |
| text.py live LLM 调用 | stub | 走 chat LLM 出口（VR_LLM_*），S008/S017 接 live |
| feature_interface live 取数 | 空 DataFrame | S008 数据层迁移接 live 填行，schema 已冻结 |
| 真实 10-code baseline 录制 | fallback 临时 | live 一次性录制（人工/live CI），现用 fallback JSON 验映射 |
| selection SHAP/Boruta 真接 | 降级路径 | shap/borutapy 未安装（依赖增减待用户决定），现用 permutation_importance + RandomForest |

## 6. 与 S017 对接

- `feature_interface.get_features(head, stage, t) -> DataFrame` 已冻结列 schema，S017 train/predict 经此接口取数。
- HEAD_FEATURE_SUBSETS 定义 short_sector(21)/mid_long(7)，S017 heads 按此消费。
- 特征快照待 S017 train.py 落地时固化进模型产物（保证可复现）。

**结论**：S018 第一批特征工程（22 特征 + 注册表 + 选择 harness + 集成接口）已实现并全测试通过，合规自查逐条确认未触边界。macro 第二批与 live 取数随外部数据源/S008 解除后补登。
