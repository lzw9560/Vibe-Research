# S055 任务拆分（原子任务 + 依赖 + 验收）

> 分支：`feature/S055-seal-intraday-alert` off develop；squash 合并；live 冒烟通过前不合。
> 验收通用项：每个后端任务带 pytest -m "not live"；前端任务带 tsc + vitest。

## T1 采集层（无外部依赖，先落库可离线验证）

| # | 任务 | 依赖 | 验收 |
|---|------|------|------|
| T1.1 | SQLite 表 `seal_intraday_snapshots` schema + migration（ts/date/code/name/pool/price/seal_amount/open_count/first_seal_time/consec_boards/sector + 索引 date,code,ts），库路径遵 S037 惯例 | — | migration 幂等测试 |
| T1.2 | 采集函数：`em_zt_topic_pool()` → 快照行写入（走 em_get 限流/熔断；东财失败记降级不抛） | T1.1 | mock 东财响应单测（成功/超时/空池三态） |
| T1.3 | 指数快照采集：`tencent_quote` 批量（C4 输入）+ 候选股流通市值缓存（C6 输入，TTL 复用） | T1.1 | mock 单测 |
| T1.4 | 交易时段门控（09:25-15:05）+ `scheduled_tasks` 注册（每分钟 tick 内按采集间隔触发） | T1.2 | 非交易时段零请求断言 |
| T1.5 | prune：按交易日保留 N 日（默认 30，可配） | T1.1 | 边界单测 |
| T1.6 | `config.py` 配置项：采集间隔（默认 60s 下限 30s）/保留天数/通知开关 | — | 默认值测试 |

## T2 端点

| # | 任务 | 依赖 | 验收 |
|---|------|------|------|
| T2.1 | `GET /api/risk/seal-snapshots?date=&code=`（单股时序，缺数据返空 + data_status） | T1.1 | 端点测试 |
| T2.2 | `GET /api/risk/bomb-alerts?date=`（活跃 + 历史） | T3.1 | 端点测试 |

## T3 规则引擎

| # | 任务 | 依赖 | 验收 |
|---|------|------|------|
| T3.1 | `BombAlertSystem` 重构：时序窗口输入（从快照表读近 N 分钟），保留现有 check() 兼容 | T1.1 | 现有测试不回归 |
| T3.2 | C1 封单 5 分钟降>30%（黄） | T3.1 | 合成时序：触发/临界/缺窗口 |
| T3.3 | C3 同板块龙头炸板（红）：板块内最高连板股进入炸板池判定 | T3.1 | 同上 |
| T3.4 | C4 大盘 5 分钟急跌>0.5%（红） | T1.3 | 同上 |
| T3.5 | C5 开板 3 分钟未回封（红）：open→reseal 状态跟踪 | T3.1 | 同上 |
| T3.6 | C6 封单<流通市值 0.3%（红） | T1.3 | 同上 |
| T3.7 | C2 评估：mootdx 分笔/五档可得性验证；不可得 → 「封单骤降≥50%」代理规则 + 文案显式标注降级 | T3.1 | 评估结论记录 + 代理规则单测 |

## T4 告警治理

| # | 任务 | 依赖 | 验收 |
|---|------|------|------|
| T4.1 | 去重：同股同规则冷却 10 分钟 | T3.* | 冷却窗口单测 |
| T4.2 | `notification/` 接线 + 开关（默认关） | T4.1 | 开关断言 |

## T5 前端

| # | 任务 | 依赖 | 验收 |
|---|------|------|------|
| T5.1 | API 封装 + TS 类型 | T2.* | tsc |
| T5.2 | 涨停页预警横幅（红/黄分级 + 时间 + 依据文案） | T5.1 | vitest + 走查 |
| T5.3 | 个股封单额 sparkline | T5.1 | vitest |

## T6 收尾

| # | 任务 | 依赖 | 验收 |
|---|------|------|------|
| T6.1 | live 冒烟：交易时段 ≥30 分钟运行，快照持续写入 + 限流日志正常 + 复现/解释预警 | T1-T4 | 冒烟记录进验收报告 |
| T6.2 | 验收报告（逐条 AC + 合规自查） | 全部 | specs README 归档格式 |

## 关键路径

T1.1 → T1.2/T1.3 → T1.4 → T3.1 → T3.2-T3.6 → T4.1 → T6.1；T2/T5 可与 T3 并行。
