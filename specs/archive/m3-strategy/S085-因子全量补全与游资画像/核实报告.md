# S085 核实报告 · spec 前提对代码核实

> 日期：2026-08-19
> 依据：9-agent 并行只读核实 workflow（run w4u6wxhtw / wf_bf19aac4-ced，314k tokens，156 tool calls，17 断言逐条）
> 目的：S085 spec 标「草案」，实现前先核实其对现有后端代码的事实断言，避免基于错误前提写码（先查前提 + 坚实数据地基）。
> 结论：17 断言中 confirmed 7、partial 9、refuted（子断言）若干。spec 约四成前提有问题，**不能照原样实现**。
> 决策（用户 2026-08-19）：改 spec + 零承重先行。本报告即「改 spec」产物；零承重组 A7/A8/A2/B2/B1 先实现；承重组 A1/A4/C3/A2d/A5 + 设计冲突 C1/C2/C4 等用户拍板。

---

## 0. 裁决汇总（按 spec 任务 ID）

| ID | 断言（spec 原文要点） | 裁决 | 承重 | 落地动作 |
|---|---|---|---|---|
| A1 | build_indicator_set 缺 `ind.seal_amount=pool_item.get("fund")` | partial | **是(capped)** | 🔴 defer：落点错（无 pool_item 参+fund 命名碰撞），正解 build_diagnosis_card 注入 |
| A2 | dragon_tiger mapper 丢 seats / 模型无 seats / eastmoney 已返 / fund_flow 不传 date | confirmed(4) | 否(增量) | 🟢 A2a/b/c 可做（新建独立 Seat 模型，勿复用 BillboardDetail）；A2d defer（T-1 不准+消费方广） |
| A3 | K线派生+auction 盘前走 tencent 当日恒 None | partial | 中(R3) | 🔴 defer：activity 字段盘前 None 对，但 auction_open_pct 来自 auction_screener 非 tencent，spec 机制归因错 |
| A4 | prev_amount_yi 盘前 None | confirmed | **是(放量比)** | 🔴 defer：钩子已有（yesterday_date 未传 fetch_activity）；要改 test_s084 契约 |
| A5 | 板块资金 raw akshare 非 em_get | confirmed | 中(R2 sector) | 🔴 defer：5min 缓存缓解，修法复用 push2+push2delay 双 host |
| A6 | stock_fund_flow_120d 无 date | confirmed | 中 | 🔴 defer：push2his「最近N日」语义，加 date 要改 lmt/换端点 |
| A7 | GeneScore 无 per-score date 戳 | confirmed | 否 | 🟢 做：3 步（模型加 date 字段+data.py hydrate+构造点设值），零 winrate |
| A8 | northbound 停更恒 None | partial | 否 | 🟢 做（已落地）：fund_flow.py:65 已标 missing；确认标注，不强删（历史回填依赖） |
| B1 | board_ladder 4 率采集丢弃，market_context 透传 | confirmed | 否(设计冲突) | 🟢 做（定调见 §2）：FunnelResult 加 market_context=run 级市场上下文，非个股字段 |
| B2 | seat_detail 未接 DiagnosisCard | confirmed | 否(设计冲突) | 🟢 做（定调见 §2）：seat_detail=聚合 only，不输出个体席位名 |
| B3 | seal_delta 不透传 | partial | 否 | 🔴 defer：字段缺属实但来源标错（.delta 非 .seal_delta）+ 只写不读孤儿，须加 reader |
| B4 | kline bars 只取 10 | partial | 否 | 🔴 defer：offset=10 对，但 change_5d 等字段不存在（spec 新增非现存 bug） |
| B5 | concept_count/announcement_type 未派生 | confirmed | 否 | ⚪ YAGNI：纯派生，消费方可直接 len()，除非 API 契约要扁平字段否则不加 |
| C1 | 知名游资花名册 | (C2 同) | 否(政策) | 🔴 defer：S018 R11 政策冲突 + 营业部→花名外部知识未核实 |
| C2 | 无知名游资名单 | partial | 否(政策) | 🔴 defer：无花名对，但已有营业部级 preset；对外输出花名须推翻 S018 R11 |
| C3 | next_day_sell_rate 自然日 offset | confirmed | **是(选股分)** | 🔴 defer：→seat_type→score_modifier→选股分；长假漏判→选股分偏高；修要回溯重算+阈值重标+完整交易日历 |
| C4 | _stock_buy_sell_pairs 未沉淀 | confirmed | 否 | 🔴 defer：语义陷阱（同日买卖非 T+1）+ 两套 SeatProfile 须先定合并 |
| D1 | 竞价金额 tencent 9:25 | partial→refuted | 否 | ❌ 证伪：9:25 amount_wan=0.0（runtime 探针 .scratch/s076-quote-probe），9:26 才非 0；要 9:26+ 绕缓存或换源 |
| D2 | mootdx bids 五档 | partial→refuted | 否 | ❌ 证伪：mootdx 0.11.7 无 bids 方法（全包零五档解析），probe client.bids 是死代码；换源（东财 push2 五档/同花顺）或连 TDX 实测 |
| D3 | akshare 筹码 stock_cyq_em | confirmed | 否 | 🔴 defer：可用，但底层打东财 push2his 裸 requests（须走 em_get 防封） |
| D4 | 同花顺防封 | partial | 否 | 🔴 defer：裸 requests confirmed，但「返5字段」错（12，9 死字段可精简）；同花顺非东财域，5min 缓存节流，非硬底线 |

**计数**：🟢 零承重先做 5（A7/A8/B1/B2/A2a-c）｜🔴 承重/改 spec defer 11｜❌ 证伪 2（D1/D2）｜⚪ YAGNI 1（B5）。

---

## 1. 逐条核实详情

### A1 seal_amount 接线 — partial（🔴 defer）
- **bug 真实**：`IndicatorSet.seal_amount`（models.py:60）是孤儿字段——build_indicator_set（diagnosis.py:104-203）全函数体无 `ind.seal_amount=` 赋值；eight_standards._check_seal_ratio（eight_standards.py:174-177）`sa=ind.seal_amount`→恒 None→八项标准⑥恒 missing→DiagnosisCard.capped（diagnosis.py:227 `capped=eight.fail_count>=3`）→选股池得分封顶 55。
- **spec 落点错**：build_indicator_set 签名（diagnosis.py:104-113）**无 pool_item 参数**——funnel.py:458/527 调 `build_indicator_set(code,name,genes,activity,fund,auction,catalyst,board)` 未传 pool_item；pool_item 实为 build_diagnosis_card 参数（diagnosis.py:214），且在 check_eight_standards（L226）前可用。
- **命名碰撞陷阱（高优先）**：build_indicator_set 已有 `fund` 参数（diagnosis.py:109，资金流 source dict，键 main_net_inflow/main_net_5d/northbound 等）；zt pool 的封单额键也叫 `fund`（zt_pool_source.py:19）。`pool_item.get("fund")`（封单额）≠ `fund` 参数（资金流），实现者极易写错。
- **正解**：build_diagnosis_card 内 L226 之前注入 `if pool_item: ind.seal_amount = pool_item.get("fund")`（pool_item 来自 zt pool，非涨停股 pool_item=None→seal_amount 仍 missing，这正确，⑥仅对涨停股有意义）。
- **承重**：改 seal_amount 会改变 capped 判定，属承重数据血脉，改后须回归。

### A2 dragon_tiger seats — confirmed（🟢 A2a/b/c 做，🔴 A2d defer）
- A2a：mappers.dragon_tiger_from_dict（mappers.py:586-595）只读 records+institution，构造 DragonTiger 时只传 records 与 institution_net，全程不读 raw.get('seats')。✅
- A2b：DragonTiger（models/seat.py:24-30）只 records+institution_net 两字段，无 seats。✅ 但「复用 BillboardDetail」**错**：BillboardDetail（seat.py:33-44）字段 buy/sell/net（元）+operate_dept_name/code，而 raw seats 键 name/buy_amt/sell_amt/net（万元），字段名+单位都不同。**应新建独立 Seat 模型**贴合 raw 形状（或在 mapper 做 name→operate_dept_name、buy_amt→buy×1e4 映射）。
- A2c：eastmoney.dragon_tiger_board（eastmoney.py:387-417）已返 `seats`（buy/sell TOP5，每项 {name,buy_amt,sell_amt,net} 万元），mapper 未透传。✅
- **消费方血脉（关键）**：
  - institution_net 消费方：fund_flow.py:49→models.py:45→diagnosis.py:129（R2 漏斗因子，承重）；first_board_filter.py:979（score_dim7_institution，权重 10%）。**A2 须保 institution_net 映射（mappers.py:594）不变。**
  - records/net_buy 消费方：risk_models.py:247-253（_get_dragon_tiger_risk）、:405-410（_calculate_concentration_risk）。只读 records，加 seats 不影响。
  - RAW seats 消费方（绕过 mapper 直读 raw）：topology.py:160-179 SeatEdgeProvider——唯一已消费 seats 处，L171-175 直读 `(board or {}).get("seats",{})`。**约束：raw dict 形状不变，仅 mapper 新增透传，否则 topology 断。**
  - RAW→前端：routers/stock_data.py:231、stock_financial.py:91 直返 raw（含 seats）给前端，不经 mapper。
- A2d（🔴 defer）：fund_flow.py:48 调 dragon_tiger_board(c) 不传 date，eastmoney.py:372 默认今日。但「T-1 一致」措辞不准：funnel 传给 fund_flow 的 as_of 实为 T（date 参数），T-1（yesterday_date）仅用于 zt_pool/derived。且 spec 只说 fund_flow，实际 first_board_filter.py:977、risk_models.py:238/402 两处同样不传 date，范围比 spec 大。

### A3 K线派生盘前 None — partial（🔴 defer）
- activity 字段（max_high_pct/shadow_length_pct/ma_5_status/prev_turnover_pct）盘前走 tencent 当日路径显式置 None（activity.py:189-192），_fetch_activity_from_kline（activity.py:29）已存在但仅历史日（_is_historical_date，:158-159）走。funnel.py:355 传 date（今日）非 yesterday_date→盘前走 tencent→None。**这部分 confirmed。**
- 但「+auction_open_pct 走 tencent 当日路径恒 None」**refuted**：auction_open_pct 来自 auction_screener（auction.py:12-13，auction_screener.py:2-5 自述「盘后批量分析，非实时竞价扫描」），非 activity tencent 路径。机制归因错。
- **修复钩子已存在**：funnel.py 已算 yesterday_date（run_funnel_impl:279-286 / diagnose:499-507，经 vr_paths.last_trading_date），但只传 zt_pool_source（:297/:510）和 derived_source（:462/:532），未传 fetch_activity（:355/:491 仍传 date）。实现时把 yesterday_date 透传给 fetch_activity 即可让盘前走 kline T-1。
- **承重**：ma_5_status/max_high_pct 进 IndicatorSet 供战法层读；auction_open_pct 经 diagnosis.py:157 进 ind、funnel.py:428 进 R3 passed、_r3_triggers（funnel.py:160）判「竞价异动」——改 fetch_auction 口径影响 R3 触发，风险中等。

### A4 prev_amount_yi 盘前 None — confirmed（🔴 defer，承重）
- activity.py:201 tencent 路径 `"prev_amount_yi": None`（注释「当日路径无前日 K线 bar」）；历史 kline 路径（:134-138）算 `prev_amount/1e8`。funnel.py:355/:491 传 date 非 yesterday_date→盘前 None。
- **承重链**：activity.py:201→diagnosis.py:154（ind.prev_amount_yi）→limitup_strategy.py:922-925（`_amt_2d=getattr(indicators,"prev_amount_yi",None)` 用于放量比）。盘前 None 致战法放量比降级——实质影响点。
- **测试契约**：test_s084_subobjects.py:202-203（当日路径 None+missing 标注）、:220（历史路径 1.0）已固化，改盘前行为须同步更新。
- **修复钩子**：同 A3，把 yesterday_date 传 fetch_activity。

### A5 板块资金 em_get — confirmed（🔴 defer）
- market.py:76-82 `_sectors()` 裸调 `astock._akshare().stock_fund_flow_industry(symbol="即时")`，无 em_get/circuit_breaker。✅
- **缓解**：受 market._cached('overview') 5min TTL 保护（每 5min 仅 1 次裸调，非 per-code，符合 fund_flow.py:70 注释本意）。但仍绕过 em_get 熔断/代理探测，无 push2delay 降级（对比 eastmoney.py:229-237 market_turnover_rank 有双 host 降级）。
- **修法**：在 eastmoney.py 加 push2 clist（fs=m:90+t:2 行业板块，fid=f62 净额）走 em_get 的 sector_fund_flow，替换 market._sectors()；或复用 push2+push2delay 双 host 降级。须保 sectors[] 形状（name/pct/net/inflow/outflow/firms）不变，fund_flow.py 按 s['name']==industry 匹配。
- **承重**：_sectors()→market.get_overview()['sectors']→fund_flow.py:71-85（sector_net_inflow）→R2 fund_flow entry，**不进最终胜率/结算**（final_candidates 只读 DiagnosisCard）。

### A6 stock_fund_flow_120d 无 date — confirmed（🔴 defer）
- eastmoney.py:325 `def stock_fund_flow_120d(code: str)` 只 code，固定拉最近 120 日。fund_flow.py:27 不传日期。topology.py:104 `lambda code,_d: astock.stock_fund_flow_120d(code)` 连拓扑提供者想传 _d 也被忽略。
- **承重**：fund_flow.py:30-34 用 flows[-1]/flows[-5:] 算 main_net_inflow/main_net_5d 进漏斗因子；risk_models.py:485 读 main_net 算风险分；routers 直返前端。replay 误取最新 120 日。
- **实现成本**：push2his fflow 是「最近 N 日」语义，按历史日期精确取某日需改 lmt 或换端点，高于 dragon_tiger_board 的 date 透传。加可选 date 参数（默认 None=既有行为）可保旧调用方。

### A7 GeneScore date 戳 — confirmed（🟢 做）
- GeneScore（limitup_screener/models.py:33-56）无 date/score_date 字段。date 只在容器 ScreenerResult（:62）与 DB 表 gene_scores（migration v1:5 `date TEXT NOT NULL`, PK(date,code)）。
- data.py:157-170 load_gene_scores 重建 GeneScore 时未 hydrate row["date"]（:165 last_zt_dates=[] 占位，date 列丢弃）。
- position_advisor_v2.py:141 _latest_gene_map「今日无则取 DB 最新日」即踩 stale 口径：回退取到的 GeneScore 自身无日期，下游无法判陈旧。
- **修复 3 步**：(1) models.py GeneScore 加 `date: str=""`（带默认保兼容）；(2) data.py:157 hydrate `date=row["date"]`；(3) 构造点 compute_gene_score（models.py:247 签名需扩 date）/kline_rebuild.py:199/strategy_funnel_registry.py:363/limitup_strategy.py:345,443,480 设 date。
- **承重**：结算/胜率读 final_candidates 不读 GeneScore，加 date 不改评分逻辑、只增新鲜度 gate，对 winrate 零影响。entry 侧消费方（position_advisor_v2 回退、recommendation_engine._check_avvert_conditions、strategy_matcher.get_best_strategy）拿到 date 后可自检陈旧。
- **附带发现**：load_gene_scores 还把 last_zt_dates 硬置 []（:165），backtest_points/backtest_summary/seal_amount/float_shares/seal_to_float_ratio/limit_up_price/limit_down_price 无对应 DB 列（save_gene_scores 只存 16 列）→DB→内存 round-trip 恒丢失。pre-existing 缺口，非 A7 范围，但若要让 GeneScore 真正可回溯需一并考虑（本次不动，登记）。

### A8 northbound 停更 — partial（🟢 做=确认已落地）
- predict/features/fund_flow.py:214-248 fetch_northbound docstring：「2024-08-19 北向规则变更后个股日级北向数据停更…近期日期取不到行→返 None」。走 em_get-backed eastmoney_datacenter（:227，防封 OK）。
- **诚实标停更已落地**：candidate_funnel/sources/fund_flow.py:65 `entry["missing"]["northbound"]="北向未取得（2024-08-19 后个股日级北向停更/当日无数据）"`。
- 「恒 None」过强：历史回填（pre-change）有数据；date=None 取 rows[0] 可能取陈旧 2024 值（live funnel 恒传 as_of 走 date 匹配返 None，当前安全）。
- **动作**：确认标注在位即可，不强删 northbound 字段（predict/features 历史回填路径依赖）。

### B1 market_context — confirmed（🟢 做，定调见 §2）
- 采集：market.py:231-234 算 seal_rate/break_rate/promotion_rate；:256-263 return dict 含 max_boards/seal_rate/break_rate/promotion_rate。
- 丢弃：board_ladder.py:43-52 `fetch_board_ladder(date)` 只取 lianban_stocks，4 率未读出。
- FunnelResult 无字段：models.py:214-224 字段=run_id/date/layers/final_candidates/threshold_config/sentiment_phase/as_of，无 market_context。
- **近乎免费**：board_ladder.py:9-10 get_market_emotion_raw「供 _fetch_market_emotion 与 STI compute 复用同一份 emotion」；workflow.py:294-298 _fetch_market_emotion 已 get_market_emotion_raw 并读 4 率（即数据已被拉取并消费）。
- **不是全局丢弃**：4 率经 shared cache 已被 briefing（_fetch_market_emotion）、STI engine（limitup_sti/service.py:142-155）、intraday_sentiment 消费；仅 board_ladder→funnel 候选路径丢弃。
- **承重**：4 率非 final_candidates 承重出口，market_context 仅展示/上下文则无血脉污染。

### B2 seat_detail — confirmed（🟢 做，定调见 §2）
- DiagnosisCard（models.py:141-162）无 seat_detail/seat_risk 子对象。✅
- seat_engine/service.py:361 `"buy_one_ratio": buy_one_ratio`（S079 R3）；strategies/hot_money_seats.py:62 `class SeatRiskFactor`。grep seat_detail/SeatRiskFactor/buy_one_ratio/hot_money_seats 于 candidate_funnel/ 全空——未接入选股池。
- **已实现范围**：seat 风险因子非全未用——strategy_funnel_registry.py:412 已 import compute_seat_risk_factor/load_aggregate_profiles/fetch_billboard_for_date 并在策略漏斗消费；dragon_tiger_seat_filter.py:153-164 用 seat_engine.compute_consensus_signal 取 buy_one_ratio、:263 独食独大、:354 仓位砍半。即 seat 引擎已实现且在策略层用，仅未浮到 candidate_funnel.DiagnosisCard。
- **承重**：seat 数据目前不影响 capped/胜率/结算，加它属增量呈现，风险低。

### B3 seal_delta — partial（🔴 defer）
- B3a：compute_seal_trajectory（intraday_features.py:271）算 `delta=last_seal-first_seal`，但返 SealTrajectory dataclass 字段名 `delta`（:226），**非 seal_delta**。字面 seal_delta（dict key）由 compute_trajectory（:83）产出 `{"seal_delta":...}`（:90）。两函数同语义（末-首），不同暴露形态。
- B3b：derived_source.fetch_derived 只走 R7 路径（compute_derived_features，输出无 seal_delta）；get_derived_result（seal_intraday_collector.py:218-245）只返 {last_lock_time,broken_duration_min,max_drop_pct,limit_price,data_status}，无 seal_delta；仅 fallback（derived_source.py:44）compute_derived_features 返 seal_delta 且仅当预采集表无行才触发。IndicatorSet 无 seal_delta 字段。✅ 不透传确认。
- **孤儿陷阱**：intraday_features（R1 trajectory）表是只写不读——persist_trajectory（:179-193）写 seal_delta（:187 列+:190 traj["seal_delta"]），scheduled_tasks.py:895 调，但全仓 grep `FROM intraday_features`/`get_trajectory_result` 空集，无 reader。甚至 compute_seal_trajectory:258-261 都不读该表，重 SELECT 原始 seal_intraday_snapshots 重算。**加字段不接 reader 就重蹈 seal_amount 孤儿覆辙。**
- **实现路径**：(a) collector 加 get_trajectory_result(code,date) 读 intraday_features 表，funnel relay 并入 IndicatorSet【推荐】；(b) 塞进 R7 派生路径【不推荐，混淆 R1/R7 分表设计】。
- **承重**：seal_delta 当前不进 capped/胜率/结算，属增量因子；但若 spec 意图让八项标准或战法因子用它则属承重，须明确消费方。

### B4 kline bars 只 10 — partial（🔴 defer）
- activity.py:66 `bars=astock.kline(c,4,10) or []`，offset=10 即 10 根。✅ mootdx offset 上限 800（stock_data.py:188），250 可取。
- 但 change_5d/change_10d/change_20d/turnover_percentile_250d 全仓 grep 零命中——这些字段**当前代码不存在**，属 S085 spec 计划新增。「10 不足」是前瞻约束而非现存 bug。算术正确：change_Nd 需 N+1 根，turnover_percentile_250d 需 250。

### B5 concept_count/announcement_type — confirmed（⚪ YAGNI）
- models.py:50 `concepts: list[str]=[]`；IndicatorSet 无 concept_count。Announcement 有 type（:23）但 IndicatorSet 无聚合 announcement_type。
- concept_count=len(concepts) 纯派生，消费方直接 len(ind.concepts) 即可，加字段未必必要，除非序列化/API 契约要扁平字段。先确认有无消费方再建。concepts 被 eight_standards ⑦_check_hot_sector（eight_standards.py:200）用列表本身非 count。

### C2 知名游资花名册 — partial（🔴 defer，政策冲突）
- grep 赵老哥/章建平/炒股养家/作手新一 零命中。risk.py:166-176 是硬编码 mock（「某知名游资A/B」）。
- **已有营业部级 preset**：backend/data/hot_money_seats_preset.json，含 seat_groups（拉萨天团系/深圳游资系/机构专用/量化席位群），各 group 下营业部全名，仅被 hot_money_seats.py:200 merge_with_presets 消费。非游资花名。
- **政策冲突**：hot_money_seats.py:16 docstring 明确「不输出个体席位名（S018 R11：个体席位标签 alpha 已衰减，只用聚合分类）」。私有对账用花名无碍，对外输出花名须先推翻此政策。
- **营业部→花名归属**：外部市场知识，代码无法核实，标注未核实。S085 若新增花名字典须与 preset seat_groups 对账/扩展，不能从零另建。

### C3 next_day_sell_rate 真实交易日 — confirmed（🔴 defer，承重）
- hot_money_seats.py:175-185 用 1-3 自然日 offset（`bd_dt+timedelta(days=offset)`）。fetch_billboard_dates（:77-96）返 sorted(set(TRADE_DATE)) 真实有榜交易日列表，但 build_seat_profiles 未用。
- **承重链（关键）**：next_day_sell_rate→_classify_seat→seat_type（一日游/接力型）→compute_seat_risk_factor day_trip_ratio→score_modifier（×0.7/×0.9）→strategy_funnel_registry.py:460 `score=round(score*seat_risk.score_modifier,4)` 直接乘选股分。
- **bug 后果**：长假缺口>3 自然日（春节/国庆）买入方次日卖出 MISSED→next_day_sell_rate 系统性偏低→偏向接力型→少计一日游风险扣分→选股分偏高。周末勉强 catch（周五买→周一=offset3）。
- **修复影响**：改 seat 分类+score_modifier→选股结果漂移，需回溯重算+阈值重标定。fetch_billboard_dates 返「有龙虎榜数据的交易日」子集（稀疏），长假/停牌期需更完整日历（astock 交易日历）才稳。

### C4 _stock_buy_sell_pairs 沉淀 — confirmed（🔴 defer，语义陷阱）
- seat_engine/models.py:41 `_stock_buy_sell_pairs: set=PrivateAttr(default_factory=set)`（私有，非持久化）。service.py:139-140 `if buy_amt>0 and sell_amt>0: profile._stock_buy_sell_pairs.add((stock_code,seat_name))`。所有 save/return 路径 pop（service.py:200,211,228,460,520）→seat_profiles.json 永不含。SeatProfile 公开字段（models.py:27-36）无 pair-score。
- **语义陷阱**：_stock_buy_sell_pairs 记录同一条记录内 buy_amt>0 且 sell_amt>0=**同日买卖双向**（same-day round-trip），非 T 买 T+N 卖的跨日接力（后者是 hot_money_seats.next_day_sell_rate）。若 S085「习惯分析」要 T+1 卖出行为，这个现有 set 是错误代理。
- test_s008_bugs.py:119-121 有实例隔离测试，改持久化字段须同步。

### D1 竞价金额 tencent 9:25 — partial→refuted（❌ 证伪）
- D1a「未在 auction.py 使用」confirmed：auction.py:12-13 用 auction_screener，非 tencent。✅
- D1a「9:25 可取竞价金额」**refuted by runtime probe**：.scratch/s076-quote-probe/matrix_20260819.json 显示 tencent amount_wan 9:24:18=0.0（fresh）、9:25:18=0.0（cached 0.1ms 陈旧）、9:26:18 才非 0（茅台4095万/平安766万/宁德15510万/中芯11934万）。tencent.py:71 `amount_wan=num(37)`=累计日成交额；:97-101 60s TTL 缓存加剧（9:24 取过→9:25 命中缓存返 0.0）。
- **结论**：9:25 集合竞价尚未收盘/feed 未刷新，amount_wan=0.0，无法取竞价金额。9:26 仍在 9:30 连续开盘前，所以 9:26 amount_wan 是纯竞价成交额。要竞价金额须 9:26+ 绕缓存取或换源。
- D1b：bidding_monitor.py 存在（fetch_auction_snapshot_batch:91 用 astock.tencent_quote→model.turnover→auction_amount 元）；first_board_confirm.fetch_auction_data 存在（:66，auction_amount_wan 万元）。两者都有 9:25=0.0 隐患。
- **附带 GAP**：auction_screener 候选 dict（:237-254）含 seal_amount（封单额，元）但 auction.py:23-32 只取 code/name/auction_open_pct 丢弃其余。注意 seal_amount（封单额）≠ 竞价成交额，不可互换。first_board_confirm 未接 live scheduler（同步骨架）；bidding_monitor 9:15-9:25 循环采样是 TODO（:201 注释，当前单次快照，cancel_rate 硬编码 0.0）。

### D2 mootdx bids 五档 — partial→refuted（❌ 证伪）
- mootdx_src.py:32,44 仅暴露 kline(code) 与 finance(code)，无 bids。✅
- 「client.bids(symbol) 存在」**refuted**：mootdx 0.11.7 全包零买卖五档——StdQuotes hasattr bids=False、ExtQuotes hasattr bids=False（且 ExtQuotes 已废弃「扩展市场行情接口已经失效」）、底层 TdxHq_API 无 bid 方法、grep 'def bids' 无命中、5 档字段名（best_price/buy_price/bid1/买一价）全包零命中。backend/tools/first_board_quote_source_probe.py:190 的 client.bids(symbol=code) 是死代码（运行即 AttributeError 被 try/except 吞，作者自注「API 形状不确定」）。
- TDX 协议免费/不封 IP 原则成立，但拿不到五档。要五档须换源（东财 push2 stock/get 五档/同花顺）或先连 TDX 实测 mootdx quotes() 返回列（离线无法判定）。

### D3 akshare 筹码 — confirmed（🔴 defer，防封）
- akshare 1.18.64 有 stock_cyq_em（symbol,adjust），返获利比例/平均成本/90成本-低/高/90集中度/70成本-低/高/70集中度。✅
- backend 全仓无 cyq 业务消费（first_board_filter 的 _chip_structure 走 baostock 换手率/量比，非 cyq）。
- **防封风险**：stock_cyq_em 底层（akshare 源码:208）直打 https://push2his.eastmoney.com/api/qt/stock/kline/get 用裸 requests.get（:218）——这是东财 push2his 端点，直接用 akshare 绕过 em_get 防封底线（§1.2）。须复刻其 kline 拉取改走 em_get（同 host push2his），或显式承认 akshare 裸打东财的封 IP 风险。

### D4 同花顺防封 — partial（🔴 defer）
- ths_limit_up_pool 定义在 eastmoney.py:174（re-export astock.py:66），走裸 requests（:192 `r=requests.get(url,timeout=10)`），无 circuit_breaker。✅
- 「返 5 字段」**错**：:202-213 返 12 字段（code/name/price/pct/reason/board_type/seal_rate/break_times/seal_amount/high_days/first_time/is_again）。消费方只读 3：market.py:179 high_days（降级 max_boards）、market.py:240 len(ths)（zt_count 交叉验证）、first_board_filter.py:1030 + sector_cycle.py reason（题材聚合）。其余 9 死字段可精简。
- 「触东财防封底线」**存疑**：§1.2 工程底线 scope 是「东财端点走 em_get」，而 ths_limit_up_pool 打同花顺 dataapi.10jqka.com.cn（非东财域），docstring（:169-173）显式「非东财防封域，直 requests」+5min TTL 缓存限频。严格说不触东财防封底线，但裸 requests 对同花顺亦无熔断/限流（仅缓存层节流）。

---

## 2. 设计调子（零承重先做组的口径）

### B1 market_context
- **定位**：FunnelResult.market_context = **run 级市场聚合上下文层**（4 率+lianban_stocks+ladder），复用 shared cache（get_market_emotion_raw），非个股 IndicatorSet 字段。
- **S049 B 冲突化解**：board_ladder.py:4-8 注释「S049 B：个股 IndicatorSet 不再带市场级三率…全市场同值，塞个股无信息量」——这是**个股字段**剥离决策。B1 加在 **FunnelResult（run 容器）** 层，非个股 IndicatorSet 层，不与 S049 冲突。定位为「本次 funnel 运行的市场快照/审计上下文」，不是「补个股缺失字段」。
- **briefing 去重**：briefing（_fetch_market_emotion）已展示 live 市场情绪。FunnelResult.market_context 是 run 时快照（retrospective/审计用），与 live briefing 口径一致（同一 shared cache），前端须明确二者关系避免双 surface 冲突。
- **不进评分**：market_context 仅展示/上下文，不参与 capped/胜率/结算（final_candidates 仍是唯一承重出口）。

### B2 seat_detail
- **定位**：DiagnosisCard.seat_detail = **聚合 only 子对象**：{buy_one_ratio, seat_type_summary（聚合分类 counts）, score_modifier, data_status}。
- **S018 R11 冲突化解**：hot_money_seats.py:16 政策「不输出个体席位名（个体席位标签 alpha 已衰减，只用聚合分类）」。**seat_detail 不放个体营业部/席位名**，只放聚合指标。个体席位名/花名属 C1/C2（政策反转决策，用户拍板）。
- **取数**：build_diagnosis_card 内调 seat_engine.compute_consensus_signal(trade_date,code) 取 buy_one_ratio + hot_money_seats.compute_seat_risk_factor 取 SeatRiskFactor（聚合）。盘前/非涨停股无龙虎榜→None 降级（守不臆造）。
- **已实现复用**：strategy_funnel_registry.py:412 + dragon_tiger_seat_filter.py:153-164 已在策略漏斗消费这些——B2 是把它们浮到 DiagnosisCard 呈现，不是新算。

### A2 seats（独立 Seat 模型）
- **定位**：DragonTiger 加 `buy_seats/sell_seats: tuple[Seat,...]=()`（默认空，保 institution_net 映射不变）；新建 `Seat` 模型贴合 raw 形状（name/buy_amt/sell_amt/net，万元）。
- **不复用 BillboardDetail**（单位/字段名不同，B3 extra 证两口径不同源）。
- **约束**：raw dict 形状不变（保 topology.py:171-175 绕 mapper 直读 raw 不断），仅 mapper 新增 seats 透传 + 模型加字段。
- **去重**：raw seats buy_data/sell_data 有重叠（同一营业部可能买卖榜都在），mapper 透传时按 name 去重决策（实现时定）。

---

## 3. defer 清单（等用户拍板）

### 承重 bug 组（改了动选股分/因子口径，要回归+回溯）
- **A1** seal_amount 接线（capped 偏置）——正解 build_diagnosis_card 注入，须回归 capped 判定。
- **A4** prev_amount_yi 盘前 T-1（放量比降级）——须改 test_s084 契约。
- **C3** next_day_sell_rate 真实交易日（选股分偏高）——须回溯重算+阈值重标+完整交易日历。
- **A2d** dragon_tiger 传 date（R2+dim7）——T-1 措辞修正+覆盖 first_board_filter/risk_models 三处。
- **A5** 板块资金 em_get（R2 sector）——复用双 host 降级，保 sectors 形状。
- **A6** stock_fund_flow_120d date（push2his 语义改造）。
- **A3** K线派生盘前 T-1（auction_open_pct 机制厘清+R3 影响）。

### 设计冲突组（须先定调再动）
- **C1/C2** 知名游资花名册——S018 R11 政策反转决策 + 营业部→花名外部知识未核实 + 与 preset 对账。
- **C4** _stock_buy_sell_pairs 沉淀——语义陷阱（同日买卖 vs T+1）+ 两套 SeatProfile 合并决策。
- **B3** seal_delta 透传——须加 reader（get_trajectory_result），定 IndicatorSet vs card.derived 承载位。

### 证伪组（须换源/砍）
- **D1** 竞价金额——9:26+ 绕缓存或换源（非 tencent 9:25）。
- **D2** 五档——换源（东财 push2 五档/同花顺/连 TDX 实测 mootdx quotes()）。
- **D3** 筹码——走 em_get 复刻 stock_cyq_em 的 kline 拉（同 host push2his）。
- **D4** 同花顺——加同花顺专用限流/熔断或并入 transport；9 死字段精简。

### YAGNI
- **B5** concept_count/announcement_type——纯派生，确认有扁平字段消费方再加。

---

## 4. 零承重先做组实现计划（本会话）

顺序（共享 candidate_funnel/models.py+diagnosis.py，须串行，不并行 workflow）：
1. **A7** GeneScore date（limitup_screener/models.py+data.py+构造点，隔离，TDD）
2. **A8** northbound 标注确认（fund_flow.py，最小）
3. **A2** dragon_tiger seats（models/seat.py 新 Seat 模型+DragonTiger 加字段+mappers.py 透传+diagnosis.py 塞入，TDD）
4. **B2** seat_detail（models.py DiagnosisCard+diagnosis.py 聚合取数，TDD，守 S018 R11）
5. **B1** market_context（models.py FunnelResult+funnel.py 透传 shared cache，TDD）

每步跑 `cd backend && .venv/bin/python -m pytest -m "not live" --deselect tests/test_market.py::test_fetch_global_intel_wm_import_fails`（newsradar 联网测试拖垮，见 memory）。
实现完开 review workflow 对抗式复核（承重泄漏/S018 R11 合规/topology 不断/向后兼容/test 覆盖）。

---

## 5. 实现结果（2026-08-19 完成）

### 5.1 零承重组落地（21 单测全绿）
- **A7** GeneScore date：`models.py:57` 加 `date: str=""`；`data.py:170` hydrate `row["date"]`；`compute_gene_score` 加 `date` 参；7 构造点设值（service/kline_rebuild/registry/limitup_strategy×3）。零 winrate（结算读 final_candidates 不读 GeneScore）。5 单测。
- **A8** northbound：确认 `fund_flow.py:65` 已标 missing（"2024-08-19 后停更"），无需改码。
- **A2** dragon_tiger seats：`models/seat.py` 新 `Seat` 模型（name/buy_amt/sell_amt/net 万元，frozen）+ `DragonTiger.buy_seats/sell_seats`（默认空 tuple）；`mappers.py:586` 透传 raw['seats']；institution_net 映射不动（承重血脉保）。6 单测。
- **B2** seat_detail：`DiagnosisCard.seat_detail`（聚合 only，守 S018 R11——不放个体席位名）；`_build_seat_detail` 调 `compute_consensus_signal`+`compute_seat_risk_factor`。**opt-in**：bulk 漏斗 `with_seat_detail=False`（perf），单股 `diagnose()` 开（见 §5.2 review MEDIUM 修复）。6 单测。
- **B1** market_context：`FunnelResult.market_context`（4 率+lianban_stocks+date，复用 `get_market_emotion_raw` shared cache 零额外外调）；非个股字段（S049 B 边界守）。4 单测。

### 5.2 review workflow 对抗式复核结论（wu4ute6t0，2/4 agent 成功，2 个 503 挂）
无 HIGH/CRITICAL。3 个 LOW/MEDIUM：
- **MEDIUM（已修）**：`_build_seat_detail` per-code 调 `compute_consensus_signal`（3 次 datacenter/code）+ `compute_seat_risk_factor`，bulk 漏斗 N 候选会拖垮 `/api/candidates`。reviewer 建议 batch billboard，但 `compute_consensus_signal` 内部 per-code 取数无法靠 batch billboard 解决。**正解：seat_detail opt-in**——bulk `run_funnel_impl` 默认 `with_seat_detail=False` 跳过（选股池列表快），单股 `diagnose()` 开 `with_seat_detail=True`（1 code 开销可接受）。已实现 + 加 bulk-skip 测试。
- **LOW（已记）**：A7 `GeneScore.date` 全链路写入但零消费方读取（预留 dead field）。entry 侧陈旧自检消费方（position_advisor_v2 回退/recommendation_engine/strategy_matcher）待接——字段已就位，消费方后续接（不臆造，标"字段预留，消费方待接"）。
- **LOW（pre-existing，非 S085）**：`win_rate.py:297` `fc.get('gene_score')` 回退受 S084 R1 副作用（gene_score 现为 dict 非 float），gene_map miss 时返 dict。S084 遗留，非我 A7 引入——登记，待 S084 清理。

### 5.3 接手处理的 pre-existing 失败（12 个，全 HEAD b0b43d7 上预存，非 S085 引入——stash 验证）
用户指示"接手处理"（当前唯一活跃会话）。逐个判 stale-test vs real-bug（不盲目改测试放水）：
- **test_regression_bugs test_all_task_types_registered**：3 新 task type（derived_precompute/first_board_quote_probe/zt_history_snapshot，S076/S078/S084 合法注册）测试期望集没跟上。**stale test，修期望集**。
- **test_s008_t13a test_activity_output_shape_unchanged**：tencent 路径 `prev_amount_yi` 盘前 None→missing（S084 R4.3 加的 K线派生字段，正是 S085 A4 defer 点）。测试断言"无 missing"是 pre-S084 旧契约。**stale test，改断言为 basic 字段不 missing（K线派生允许 missing）**——不预判 A4（A4 改 value 非 shape）。
- **test_s050_shadow_comparison（4）**：fixture 用固定日期 2026-07-20/21，但 `_shadow_comparison_impl` 用 `today=datetime.now()` 算 28 天窗口——今天 08-19，07-20 已 30 天前出窗，rows 全过滤→follow=0。**date-aging stale test，改相对日期**（`today - timedelta`）。
- **test_s058/s062 strategy cards（3）**：STRATEGY_REGISTRY 11 战法，cards/ 只 9 个 .md——缺 `weak_turn_strong`+`pattern_reversal`（S081 注册的战法，card 文件没建）。**真 gap，建 2 个 card**（按 first_plate.md 格式，含适用天气/入场/退出/风险/免责）。
- **test_s004 R3DegradationContract（2）**：S084 reframe（funnel.py:411-413 注释明示）"R3 不再过滤竞价/催化——下放战法层…原 exp-3 降级已成默认行为"。测试断言旧契约（data_status="降级"/R3 过滤到 1）。production 意图明确（代码注释写死，非路线图未定）。**stale test，更新断言到 S084 新契约**（R3 pass-through，data_status="R3 已下放战法"，auction 存在也不过滤）。
- **test_workflow_snapshot test_collect_done_writes_snapshot_file**：单跑过（5.9s），全量挂——测试顺序/shared-state 问题（非我代码 bug）。flaky-by-ordering，不深挖（登册）。

净结果：11/12 pre-existing 修绿 + S085 21 绿。workflow_snapshot 留 flaky-by-ordering（单跑过）。

---

## 5.4 承重 bug 修复（2026-08-19，用户"先修bug"推进）

按"代码侧明确 + 不需 live probe + 不碰 S084 semantic + 不需回溯"筛 4 个先修，各 TDD + 回归绿（257 过）：

### A1 seal_amount 接线（capped 偏置）— 修
- 落点：`build_diagnosis_card`（diagnosis.py）check_eight_standards 之前注入 `ind.seal_amount = float(pool_item.get("fund"))`（非 spec 说的 build_indicator_set——它没 pool_item 参；命名碰撞守护：pool_item.get("fund") 非 fund 参数）。
- 单位：fund（封单额，元）+ float_market_cap（元）→ ratio 无量纲（核实 first_board_filter:269/ZTPoolItem:130/eastmoney:178 三处一致）。
- 效果：八项标准⑥从恒 missing 变 pass/fail → fail_count 不再偏置 → capped 判定纠偏。
- 承重：capped 是 run 时判定（不进历史 DB），零回溯影响。7 单测（test_s085_seal_amount.py）。

### A2d dragon_tiger 传 date（replay 误取）— 修
- 修：`fund_flow.py:48` `dragon_tiger_board(c, trade_date=as_of)`（修 replay 误取今日龙虎榜；盘前 as_of=T→T 日未出榜返 T-1 最近；replay as_of=H→查到 H）。
- 不加 yesterday_date 参（as_of 已有，dragon_tiger_board 查 30 天范围，传 as_of 即可——比核实报告说的"传 yesterday_date"更准）。
- 承重：institution_net → R2 factor + dim7 score。改 date 语义不改返回结构。3 单测（test_s085_dragon_tiger_date.py）+ 修 test_s008_t13e:147 stale mock（1-arg→3-arg 跟签名）。
- **todo**：first_board_filter:977 + risk_models:238/402 也直调 dragon_tiger_board 不传 date（不同模块，date 语义不同——首板流/风险评估），单独修。

### A6 stock_fund_flow_120d replay 误取（fund_flow 链）— 修
- 修：`fund_flow.py:27` 加 `if as_of: flows = [f for f in flows if f.date[:10] <= as_of]`（过滤 flows ≤ as_of，main_net 取 as_of 日行而非最新今日）。不改 stock_fund_flow_120d 签名（topology/risk_models/前端直调不受影响）。
- 承重：main_net_inflow → R2 factor（不进最终胜率/结算）。零回溯（main_net run 时算）。4 单测（test_s085_fund_flow_date.py）。
- **todo**：risk_models:485 直调 stock_fund_flow_120d（不经 fund_flow），单独修；topology:104 `_d` 被签名忽略，单独修。

### C3 next_day_sell_rate 真实交易日（选股分偏高）— 修代码，回溯 todo
- 修：`hot_money_seats.py:175` build_seat_profiles 用 sorted 真实交易日 index+1..3 替代 1-3 自然日 offset（修长假漏判：春节/国庆买入→真实下一交易日卖出，自然日 +1..3 够不着→MISSED→rate 偏低→偏向接力型→少扣一日游风险→选股分偏高）。保留 1-3 宽泛语义，只修长假 catch。
- 承重：next_day_sell_rate → seat_type → score_modifier → 选股分（strategy_funnel_registry:460）。
- **回溯 todo**：hot_money_seats.json 持久化旧 modifier（偏高），需重算历史画像统一（update_hot_money_seats 60 日重跑 + 选股分重算），需数据积累后跑。改后历史数据用旧 modifier、新 run 用新 → 不一致，必须回溯。
- 5 单测（test_s085_next_day_sell_rate.py）。

### A5 板块资金 em_get 防封 — 修（live probe 后）
- probe（2026-08-19 live）：akshare stock_fund_flow_industry 打**同花顺** dataapi.10jqka.com.cn（非东财，hexin-v 反爬 + HTML parse）；东财 push2 直连断（RemoteDisconnected），push2delay 可达（双 host 降级必要）；东财行业板块无 inflow/outflow 字段（只 f62 净额 + f104/f105 涨跌家数）；sector_net_inflow/inflow/outflow 全 dead fields（无下游消费含前端）。
- 修：`eastmoney.sector_fund_flow()` 新建——push2 clist（fs=m:90+t:2, fid=f62）走 em_get + 双 host 降级（push2→push2delay）；`market._sectors()` 改用它（替换 akshare 同花顺 raw）。
- 字段映射：name=f14 / pct=f3 / net=f62/1e8（亿）/ inflow=None / outflow=None（东财无，dead 保形状）/ firms=f104+f105。
- 性质（probe 改判断）：akshare 同花顺非东财——**§1.2 东财 scope 不强制**（A5 非东财端点违规，是同花顺端点防封）。A5 是**防封工程改进**（em_get 熔断纳入，消除 raw requests 无熔断），非选股 bug。做它代价中等、无下游影响（dead fields）、收益防封。
- 4 单测（test_s085_sector_fund_flow.py）+ market 回归 36 过。

---

## 5.5 全承重 bug 修完（2026-08-19）

A4/A3 + B3 也落地（用户"改完提交"）：

### A4/A3 prev_amount_yi + K线派生盘前 None（S084 盘前边界）— 修
- 修：funnel.py run_funnel_impl:355 + diagnose:519 盘前（date>=today）fetch_activity 用 yesterday_date（走 kline T-1 路径，算 prev_amount_yi/K线派生，修放量比降级 limitup_strategy:922）；历史日（date<today）保 date（replay 取该日）。
- 实现：`_is_pre_market = date >= today; act_date = (yesterday_date or date) if _is_pre_market else date`；diagnose 把 yesterday_date 算从 fetch_activity 后移到前。
- 承重：盘前所有因子语义变 T-1（S084「所有因子取 T-1」intent 实现）——price/change/turnover/eight_standards/战法全 T-1，影响选股分 semantic。S084 territory。
- tests 跑 run_funnel/diagnose 都用历史日（已走 kline，不受盘前分支影响）；2 单测（test_s085_pre_market_activity.py）验盘前用 yesterday、历史日用 date。

### B3 seal_delta 透传 — 修（dead field，消费方待接）
- 修：(1) IndicatorSet 加 seal_delta 字段；(2) seal_intraday_collector 加 get_trajectory_result(code, date) reader 读 intraday_features 表 seal_delta 列（修只写不读孤儿）；(3) derived_source.fetch_derived 调 reader，seal_delta 塞 derived dict；(4) funnel mutate ind.seal_delta = derived.get("seal_delta")。
- 承重：seal_delta 不进 capped/胜率/结算（无消费方 → dead field，像 A7，review LOW）。价值 = 只写→可读+透传，为未来消费方预留。6 单测（test_s085_seal_delta.py）。

### 承重 bug 修完汇总（7 个）
A1（capped）/A2d（dragon_tiger replay）/A6（fund_flow replay）/C3（next_day_sell_rate 长假，**回溯 todo**）/A5（板块资金 em_get 防封）/A4-A3（盘前 T-1 semantic）/B3（seal_delta reader，dead）。

**回溯/单独修 todo**：
- C3：重算 hot_money_seats.json 历史画像（update_hot_money_seats 60 日重跑 + 选股分重算），需数据积累后跑。
- A2d/A6 残留：first_board_filter:977 / risk_models:238,402,485 / topology:104 的 dragon_tiger_board + stock_fund_flow_120d 不传 date（不同模块，date 语义不同，单独修）。
- B3：消费方待接（entry 侧/战法读 ind.seal_delta 或 card.derived.seal_delta）。

### 证伪组（D1/D2/D3/D4）— 需换源/研究，非 bug fix
- D1 竞价金额：tencent 9:25=0.0（runtime 证伪），须 9:26+ 绕缓存或换源。
- D2 五档：mootdx 0.11.7 无 bids（证伪），须换源（东财 push2 五档/同花顺/连 TDX 实测）。
- D3 筹码：akshare stock_cyq_em 底层打东财 push2his 裸 requests，须走 em_get 复刻。
- D4 同花顺防封：裸 requests 无熔断，须同花顺专用限流或并入 transport。

---

## 5.6 改动文件清单（承重 bug 修复追加）

- backend/candidate_funnel/diagnosis.py（A1 seal_amount 接线，build_diagnosis_card 注入）
- backend/candidate_funnel/sources/fund_flow.py（A2d dragon_tiger 传 as_of + A6 flows 过滤 as_of）
- backend/strategies/hot_money_seats.py（C3 真实交易日 index+1..3）
- backend/tests/test_s008_t13e_misc.py（A2d stale mock 3-arg）
- backend/tests/test_s085_seal_amount.py（A1，7）
- backend/tests/test_s085_dragon_tiger_date.py（A2d，3）
- backend/tests/test_s085_fund_flow_date.py（A6，4）
- backend/tests/test_s085_next_day_sell_rate.py（C3，5）
- backend/data/sources/eastmoney.py（A5 sector_fund_flow）
- backend/market.py（A5 _sectors 改用 sector_fund_flow）
- backend/tests/test_s085_sector_fund_flow.py（A5，4）
- backend/candidate_funnel/funnel.py（A4/A3 盘前 act_date 分支 + B3 mutate ind.seal_delta）
- backend/tests/test_s085_pre_market_activity.py（A4/A3，2）
- backend/candidate_funnel/models.py（B3 IndicatorSet.seal_delta）
- backend/candidate_funnel/sources/derived_source.py（B3 透传 seal_delta）
- backend/risk/seal_intraday_collector.py（B3 get_trajectory_result reader）
- backend/tests/test_s085_seal_delta.py（B3，6）

**回溯 todo（C3）**：重算 hot_money_seats.json 历史画像（update_hot_money_seats 60 日重跑 + 选股分重算），需数据积累后跑。
**单独修 todo（A2d/A6 残留）**：first_board_filter:977 / risk_models:238,402,485 / topology:104 的 dragon_tiger_board + stock_fund_flow_120d 不传 date（不同模块，date 语义不同，单独修）。

---

## 6. 改动文件清单

**S085 零承重组（实现）**：
- backend/limitup_screener/models.py（GeneScore.date + compute_gene_score date 参）
- backend/limitup_screener/data.py（load hydrate date）
- backend/limitup_screener/kline_rebuild.py（date 戳）
- backend/limitup_screener/service.py（compute_gene_score 传 date）
- backend/strategies/strategy_funnel_registry.py（_cand_to_gene date）
- backend/limitup_strategy.py（GeneScore 构造点 date ×3）
- backend/models/seat.py（Seat 模型 + DragonTiger.buy_seats/sell_seats）
- backend/data/mappers.py（dragon_tiger_from_dict seats 透传 + Seat import）
- backend/candidate_funnel/models.py（DiagnosisCard.seat_detail + FunnelResult.market_context）
- backend/candidate_funnel/diagnosis.py（_build_seat_detail + build_diagnosis_card with_seat_detail opt-in）
- backend/candidate_funnel/funnel.py（market_context 透传 + bulk 不传 trade_date + diagnose 开 with_seat_detail）

**S085 测试（新增）**：
- backend/tests/test_s085_genescore_date.py（5）
- backend/tests/test_s085_dragon_tiger_seats.py（6）
- backend/tests/test_s085_seat_detail.py（6，含 bulk-skip）
- backend/tests/test_s085_market_context.py（4）

**接手 pre-existing 修复**：
- backend/tests/test_regression_bugs.py（task types 期望集 +3）
- backend/tests/test_s008_t13a_engines.py（activity missing 断言改 basic 字段）
- backend/tests/test_s050_shadow_comparison.py（固定日期→相对日期）
- backend/tests/test_s004_funnel_perf.py（R3 契约断言更新到 S084 reframe）
- backend/strategies/cards/weak_turn_strong.md（新建）
- backend/strategies/cards/pattern_reversal.md（新建）

**spec 文档**：
- specs/S085-因子全量补全与游资画像/核实报告.md（本文件）
- specs/S085-因子全量补全与游资画像/spec.md（状态指针）
- specs/S085-因子全量补全与游资画像/tasks.md（核实状态指针）

---

## 7. todo 全清单 closed（2026-08-20 落档）

### 7.1 已实现（16 项，commit 6b7259d + e72e4ed，全量回归 2012 过）

**commit 6b7259d**（零承重 5 + 承重 7 + 接手 pre-existing 11）：
- 零承重：A7 GeneScore date / A8 northbound / A2 dragon_tiger seats / B2 seat_detail(守S018R11,opt-in防perf) / B1 market_context(守S049)
- 承重：A1 seal_amount→capped纠偏 / A2d dragon_tiger date / A6 fund_flow date / C3 next_day_sell_rate真实交易日 / A5 板块资金em_get / A4-A3 盘前T-1 / B3 seal_delta reader
- 接手 pre-existing 11/12：task types/activity shape/s050 date-aging/R3 contract S084 reframe/2缺card

**commit e72e4ed**（A2d-A6 残留 + D2 + D4 + A7 消费方，调查→实现 workflow 并行 + 对抗 verify ship）：
- A2d-A6 残留：first_board_filter:695+977 传 dragon_tiger_board(trade_date=_iso(date))（agent 抓到 YYYYMMDD→ISO 转换坑，否则 strptime ValueError→静默 50）+ topology:104 传 d（stock_fund_flow_120d 加可选 date 过滤≤date）
- D2 bids：eastmoney bids() push2delay 优先+双 host+akshare fields（触发五档自动返回），vol×100 手→股。astock re-export。
- D4 ths 防封：_ths_get（get_breaker("ths")独立限流+0.5s+抖动+threading.Lock 防并发击穿），ths_limit_up_pool 走 _ths_get + 9 死字段精简到 {code,reason,high_days}。
- A7 消费方：position_advisor_v2 _latest_gene_map 返 (gene_map,data_date,data_stale)，binary gene.date!=today 标注 stale（不阻塞仓位/win_rate），advise_recommendations/watchlist/holdings 注入 extra+risk_notes。

**e72e4ed 改动文件**：
- backend/data/sources/eastmoney.py（A6 stock_fund_flow_120d date + D2 bids + D4 _ths_get/ths_limit_up_pool + Lock）
- backend/strategies/first_board_filter.py（A2d #1+#695 date 转 ISO）
- backend/routers/topology.py（A6 #5 传 d）
- backend/strategies/position_advisor_v2.py（A7 stale 标注）
- backend/astock.py（D2 bids re-export）
- backend/tests/test_s085_score_dim7_date.py（A2d #1，含 YYYYMMDD→ISO 转换测）
- backend/tests/test_s085_topology_fflow_date.py（A6 #5）
- backend/tests/test_topology.py（mock 加 date=None 兼容）
- backend/tests/test_s085_bids_ths.py（D2+D4，17 测试）
- backend/tests/test_s008_sources_facade.py（D4 re-export）
- backend/tests/test_s085_advisory_gene_stale.py（A7，6 测试）

### 7.2 defer/wontfix（5 项，用户 2026-08-20 决策）

- **risk_models #2/#3/#4 — wontfix**（live-only）：risk_models 是实时模块（update_one_day_risk_realtime V2.0.2 动态化，:199 硬编码今日，4 调用方全不传 date，S085 作者显式 deferred）。实时风险取最新对，不 replay。Option B live-only wontfix（不修）。若未来要 replay-aware：Option A 透传 as_of 经 4 调用方 + dashboard date 绑 per-stock risk（当前 latent 不一致：risk.py:42 date 只绑 gene scores，risk 仍取今日）。
- **B3 seal_delta 消费方 — defer**：dead field 无明确消费需求。三候选（quality_standards 第9条封单衰减 / weak_turn_strong 因子 / risk position_manager 封单衰减风险）都要 spec 定阈值+判定+受影响文件+验收，非机械接线。等消费需求明确再开 spec。
- **D3 cyq 筹码 — defer**：akshare stock_cyq_em 不可包 em_get（raw requests 无注入点），须算法复刻 CYQCalculator（JS→Python ~200 行）+ 数值等价校验（对账 akshare <1e-6）+ 对齐 notification_formatters 键。中等工作量。无现成消费方（chip_profit_ratio dead），等消费需求。
- **D1 竞价金额 — defer**：tencent 9:25 amount_wan=0.0（runtime probe 证伪，.scratch/s076-quote-probe/matrix_20260819.json）。spec 前提「tencent 9:25 可取竞价金额」是假。修须 timing 设计（9:26+ 绕 60s 缓存取，或换源）。timing/换源要产品决策。
- **C3 回溯 — defer**（代码已修，数据待重算）：next_day_sell_rate 真实交易日代码已修（commit 6b7259d），但历史 hot_money_seats.json 用旧 modifier（偏高）。重算须 update_hot_money_seats 60 日重跑（em_get ~18 次 60 日龙虎榜）+ 选股分重算。需数据积累+时间，等用户要时跑。

### 7.3 证伪组 verdict
- D2 五档：✓ done（换源东财 push2delay+akshare fields，mootdx 0.11.7 无 bids 证伪）
- D4 同花顺防封：✓ done（_ths_get 限流+Lock）
- D1 竞价金额：defer（tencent 9:25=0.0 证伪，需 timing/换源）
- D3 筹码：defer（算法复刻，无消费需求）

### 7.4 调查 workflow 证据（2026-08-19，run wf_2891e850-992）
5 agent 并行调查 A2d-A6 date 语义 / D2 五档 live probe / D3 cyq 可行性 / D4 ths 方案 / B3-A7 消费方。输出 journal: subagents/workflows/wf_2891e850-992/journal.jsonl。关键发现：
- A2d #1+#695 date 是 YYYYMMDD（非 ISO），dragon_tiger_board strptime %Y-%m-%d 裸传 ValueError→静默 50（agent 抓到，我初版漏，已修加 ISO 转换）。
- D2 push2 实时稳定封（5 次全 RemoteDisconnected），push2delay 可达 + akshare fields 触发五档自动返回（最小/空 fields 返空）。
- D3 akshare 不可包 em_get（raw requests 无注入点），须算法复刻。
- D4 同花顺非东财域，em_get 不能包，须专用 get_breaker("ths") 限流。
- A7 position_advisor_v2 是唯一需 gene.date 陈旧自检的路径（recommendation_engine 走 ScreenerResult.data_freshness 冗余，strategy_matcher 战法正交）。
- B3 三候选消费方都要 spec 定阈值，非机械接线。
