# Spec: S095 — gene_scores 写路径修复与日期自证守卫

> 状态：✅已实现（2026-08-23）（commit a6618df/ac22bac/9907e8a；6 tests 全绿；全量 2226 passed；08-13~08-21 七日 7/7 code 集合全等；ora-8 终审闭合）
> 作者：opencode 会话  日期：2026-08-23
> 级别：**medium**（写路径修复 + 守卫，不改业务算法，不碰前端）
> 流程门：spec.md + plan（并入本文件）；issue 层单轮审查；直接 develop 提交
> 依赖：日期完整性专项已落（今天的全项目 is_trading_day 守卫 + 东财静默回退修复）
> 承接：S094 §10 S0 "前置硬门"（S094 的 S2/S4 合并前必须完成本 spec）

## 0. 起因（实证根因）

gene_scores 表历史行**系统性错位一天**（Oracle 两轮独立复现，08-13 起每一天）：

| gene_scores 行标签 | 实际名单 = | 写入时刻（updated_at） |
|---|---|---|
| 08-17 周一（63只） | **08-16→实为 08-14 周五**的池 63 只 | **08-16（周日）16:00** |
| 08-18 周二（106只） | 08-17 周一的池 106 只（全等） | 08-17 周一 16:11 |
| 08-19 周三（79只） | 08-18 周二的池 79 只（全等） | 08-18 周二 16:13 |
| 08-20 周四（36只） | 08-19 周三的池 36 只（全等） | 08-19 周三 17:46 |
| 08-21 周五（54只） | 当日池（唯一正确） | 08-22 17:28（fix-18 重算） |

**根因机制**（三个事实拼合）：

1. `gs(T)` 的 code 集合 ⊆ `zh(prev_trading_day(T))` —— 标签是 T，内容是**前一个交易日**的池
2. `gs(08-17)`（周一）写于 **08-16 周日 16:00**——周一交易尚未发生，此时请求"周一的池"，东财无此数据
3. 东财对"未来日期/无数据日期"请求**静默返回最近一个已定型池**（08-14 周五），代码未校验数据日期，直接以请求日期 T 标写入

**核心：这不是"非交易日"问题**（那种今天的 is_trading_day 守卫已修）——是**时序问题**：在 T 日池**尚未收盘定型**时（盘前/周末/任意未来时点）请求 T 的池，静默回退 + 未自证日期 → 错位标写入。`is_trading_day(T)=True` 的交易日同样中招（因为请求时刻早于收盘定型时刻）。

**影响**：FLOW A 候选输入（`workflow.py:182 load_gene_scores`）+ 基因得分 + R12 confidence 复用 + sector_cycle `aggregate_sectors`（按 date 读 gene_scores）+ backtest_lite/forward_test 全部坐在错位数据上。是 S094"坚实底座"的阻断前提。

## 1. 需求清单

### A. 写路径日期自证守卫（核心）

- [x] R1 `_compute_and_cache_async`（limitup_screener/service.py:188）落盘前加**数据日期自证校验** ✅已实现（`_assert_not_future_date` service.py L67）：
  - 请求日期 `target_date` 若 > 最近交易日（`last_trading_date_str()`）→ **拒绝写入**，返回空/降级结果（未来日期必然无真实数据）✅三入口统一
  - 请求日期 ≤ 最近交易日但**池尚未定型**的窗口（当日盘中）→ 按既有"盘中口径"逻辑走（现状保留），交叉校验钩子告警（R4 实现）
- [x] R2 **未来日期拒绝**是硬闸门：`_compute_and_cache_async`/`precompute_daily_async`/`precompute_daily` 三入口统一拒写返空（不查东财）✅已实现（L271/L530/L563）
- [x] R3 调用方诚实降级：**处置（ora-8 MEDIUM #6）**——实施时逐一核验 12 处调用方无误传未来日期风险（pre_market_workflow 用 last_trading_date_str/回填循环回溯过去/读路径被内部守卫覆盖），以"风险确认"替代逐处补日志；调用方代码零改动，不留"写了没做"

### B. 数据日期自证机制（防未来再犯）

- [x] R4 交叉校验钩子 ✅已实现（`_cross_check_zt_history` service.py L90）：与 `zt_history.db` 同日快照比对行数，不一致告警放行；已有 final 快照且行数不一致 → **拒绝写入**（口径说明：实现只比行数不比 code 集合——行数不等价于集合不等，拒绝条件不变；盘中收缩合法故非 final 只告警）
- [ ] R5 `save_gene_scores` 加 `pool_date` 元数据列（可选增强）——推迟（spec §8 盲点 2 拍板，S1/S2 防线已足够，S094 实施后按需加）

### C. 调用方清理（根绝误传未来日期）

- [x] R6 梳理所有写路径调用方 ✅处置（ora-8 MEDIUM #6）——逐处核验：`pre_market_workflow.py:121` 用 `last_trading_date_str()`（永 ≤ 最近交易日）；`scheduled_tasks.py:614` 回溯方向"过去"+is_trading_day 守卫；`backfill_history.py:204` read 路径 + 显式未来日期时守卫返空降级（正确行为）；其余 10 处读路径被内部守卫覆盖。**调用方代码零改动**
- [x] R7 回溯方向确认是"过去" ✅处置（ora-8 MEDIUM #6）——回溯逻辑天然向过去（`range(back_days)` 减日期），无未来风险；原承诺的"测试锁定"改为由 AC3b（拒绝方向测试）+ is_trading_day 守卫测试覆盖等价语义，不另加重复测试

### D. 历史数据处理（已完成，本条为确认）

- [x] R8（已完成）fix-18 重算 08-17~08-21（5/5 code 集合全等）+ fix-20 补重算 08-13~08-14（2/2 全等），合计 08-13~08-21 **七日全覆盖 7/7 code 集合全等**，备份 `.bak-recompute-offset-2026-08-23` + `.bak-recompute-0813-2026-08-23`
- [x] R9 08-12 及更早的历史行：保留现状（ora-8 独立 SQL 验证：≤08-12 仍错位一天，08-11: 141 vs 58、08-12: 134 vs 92——与拍板一致）。回测窗口从 08-13 起即可（近 2 周足够 §44 复验），不扩大重算范围

## 2. 受影响文件

| 文件 | 改动 |
|---|---|
| `limitup_screener/service.py` | R1/R2 三入口未来日期拒绝 + R4 交叉校验钩子 |
| `limitup_screener/data.py` | R5 pool_date 元数据列（可选） |
| `pre_market_workflow.py` | R3/R6 盘前场景诚实降级 |
| `scheduled_tasks.py` | R7 测试锁定回溯方向 |
| `tests/test_s095_gene_scores_guard.py` | 新增：未来日期拒绝 / 盘中小时合法 / 交叉校验告警 / final 快照权威 |

## 3. 验收标准

- [x] AC1 `precompute_daily_async('2026-08-29')`（未来日期）返空结果 + 不查东财（mock 断言）✅
- [x] AC2 `precompute_daily_async('2026-08-21')`（历史交易日）正常计算 ✅（回归不破坏，2226 passed）
- [x] AC3 `precompute_daily_async(last_trading_date_str())` 当天盘中请求合法 ✅
- [x] AC3b（ora-8 MEDIUM #3 补）`_assert_not_future_date('20260824')`（未来交易日）拒绝分支直接打 ✅——补此前拒绝分支零覆盖
- [x] AC4 `precompute_daily_async('2026-08-22')`（周六）返空 ✅
- [x] AC5 交叉校验：zt_history final 快照与请求池不一致 → 拒绝写入 + 日志 ✅
- [x] AC6 pytest `-m "not live"` 全绿 ✅（2226 passed，1 pre-existing S066 失败无关）

## 4. 设计取舍

1. **只做未来日期硬闸门，不改盘中语义**——盘中快照（`now < 15:31` 请求当日）是合法业务需求（盘后预计算依赖），不是错位。错位的本质是"未来日期请求"。
2. **交叉校验是第二道防线，非唯一防线**——主防线是 R2 未来日期拒绝；R4 是深度防御。
3. **不扩大历史重算范围**——08-13 前保留现状（R9 拍板），避免大范围重算引入新风险。

## 5. 合规自查

- [x] 不臆造：未来日期拒绝写入（返空不造假）；交叉校验不匹配拒绝写入
- [x] 私有数据 .vibe-research/ 不进 git
- [x] em_get 防封：未来日期拒绝不查东财

## 6. 冲突审查

| 旧逻辑 | 旧决策 | S095 决策 | 处置 | 迁移路径 |
|---|---|---|---|---|
| `_compute_and_cache_async` 无日期自证 | 接受东财静默回退任意日期 | 未来日期拒绝 + 交叉校验 | 替换 | 三入口统一闸门 |
| 盘前算当天（`pre_market_workflow:121`) | 接受空池降级 | 保留 + 诚实降级补齐 | 共存 | 补日志 |
| 历史重算范围 | 未定 | 08-13 起（不扩大） | 拍板 | 保留现状 |

## 7. 实施（plan 并入）——✅ 全部完成

### S1 未来日期硬闸门（R1/R2）✅ commit a6618df
- `limitup_screener/service.py` 三入口 `_assert_not_future_date` 守卫（L271/L530/L563，拒写返空不查东财）
- 测试：AC1/AC2/AC3/AC3b/AC4

### S2 交叉校验钩子（R4）✅ commit a6618df
- `_cross_check_zt_history`（L90）：final 快照行数比对，不一致拒绝写入；非 final 告警放行
- 测试：AC5

### S3 调用方风险确认 + 历史数据重算 ✅ commit a6618df + ac22bac
- R3/R6/R7 处置：12 处调用方逐一核验无误传未来日期风险，调用方代码零改动
- 历史数据重算：fix-18（08-17~21）+ fix-20（08-13/14），七日 7/7 code 集合全等
- 全量回归：2226 passed（+6 新测试，1 pre-existing S066 失败无关）

### 依赖
S1 → S2 → S3（已全部完成，无未完成项）

## 8. 已知盲点

1. `zt_history.db` 的 final 快照覆盖率：08-20/08-21 是 non-final，历史日 final 覆盖不全 → R4 交叉校验只在"有 final 快照"时生效，非全量防线。接受——主防线是 R2 未来日期拒绝。
2. `pool_date` 元数据列（R5）推迟——S1/S2 防线已足够，S094 实施后按需加。
3. **盘前回退假设（ora-8 B5）**：交易日盘前（如周一 08:30 请求当日）守卫放行（last_trading_date_str()=当日），若东财此刻对当日静默回退到上个定型池，错位可能复现——这正是 08-17 行的生成机制。已拍板"不改盘中语义"（取舍 #1），盘前东财返回空已处理；若未来复现，用 live 探针确认后再评估加盘中窗口守卫。
