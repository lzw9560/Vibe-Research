# S005 验收报告

> 日期：2026-07-29
> 对应：`spec.md`、`plan.md`、`tasks.md`

## 实现范围

后端 `value_funnel/` 子包 + `routers/value_funnel.py`（7 端点）+ `app.py` 注册；前端 `pages/ValueFunnel.tsx` + `lib/value_funnel.ts` + 路由注册。

## AC 逐条核对

| AC | 内容 | 状态 | 证据 |
|---|---|---|---|
| AC1 | L1 全市场扫描（行业/主题/指数） | ✅ live | POST /run direction=白酒 → 42 候选（行业成分，ST 已剔） |
| AC2 | 四层漏斗逐层收敛 + 各层可检视 | ✅ 代码+L1 live | `funnel.run_value_funnel` 编排 L1→L2→L3→L4，每层 input/output/filtered_out；L1 实测 42→42 |
| AC3 | L2 去劣7条 + 双口径 + 豁免 + 护城河 | ✅ live+单测 | 茅台 600519：5/7 通过，abs=adj=0.7143，ROE31.93/毛利率91.34/净利率51.14/现金质量0.903；第3/7条 missing；14 单测含双口径 |
| AC4 | L3 结构化分析 + 反面论据 | ✅ 代码+typecheck | `build_analysis_skeleton` 填财务摘要/估值位置/反方占位；counter_arguments 字段 |
| AC5 | L4 四大师骨架 + 文字交 AI | ✅ 代码+live | `build_deep_skeleton` 4 视角+引导问题+ai_pending；`deep-ai` 端点双出口（cli-claude 订阅 → `run_chat_cli_stream`；API → `run_chat_stream`）。**live 实测**：600519 + provider=cli-claude → 210s → ai_pending=False，四大师文字全填（毛利率91%/净利率50%/ROE30%/强现金流，客观无买卖建议），用 claude CLI 订阅额度、免 key |
| AC6 | 豁免 A/B/C 标依据 | ✅ 单测 | test_exemption_A：营收高增+ROE低→豁免A 命中 |
| AC7 | 数据不足不适用、不补全 | ✅ 单测 | test_under_5_years/test_all_missing：不足5年多项不适用，全缺失全 missing |
| AC8 | 可复现（financial_rigor 复算） | ✅ | 每条 metric.evidence 填取数时点+年份+口径；`~/tools/financial_rigor.py` 可验算市值/估值 |
| AC9 | 与 S002 短线池独立 | ✅ | 独立子包 value_funnel/、独立路由 /api/value-funnel/*、独立页面 /value-funnel，不挂 S002 |
| AC10 | 无主观结论词 | ✅ 代码审查 | 模型/路由/页面均无"一流/非一流/劣质"等词；护城河 note 明确"系统不输出主观评分" |

## 测试

- `pytest value_funnel/tests/`：**14 passed**（models 6 + quality 8）
- 修复 1 个真实 bug：`quality.py` 的 `data_years_note` 逻辑顺序错（`<5` 被 `<10` 先捕获），已修
- 前端 `npx tsc --noEmit`：无 value_funnel/ValueFunnel 相关类型错误

## Live 实测

- 路由：`/api/value-funnel/{scan,run,result,layers,{code}/quality,{code}/analysis,{code}/deep-ai}` 全注册（/openapi.json 可见）
- `GET /api/value-funnel/600519/quality` → 茅台去劣 5/7（见 AC3）
- `POST /api/value-funnel/run {direction:白酒, stage:L1}` → 42 候选（见 AC1）
- `POST /api/value-funnel/600519/deep-ai` provider=cli-claude → 210s，ai_pending=False，四大师文字全填（claude CLI 订阅额度，免 key）—— 见 AC5
- 注：`run stage=all` 完整四层未 live 跑全（42 只 × ths 年度财务取数较重）；已加 300s TTL 缓存（`_fetch_yearly_abstract`）缓解重复请求；各层逻辑经单测覆盖，L1/L2(单只)/L4(AI)已 live 验证。

## 后续改进（2026-07-29 补）

- `quality._fetch_yearly_abstract` 加 300s TTL 缓存（`_ABSTRACT_CACHE`），stage=all 对同一标的不重复请求，缓解 akshare 限流。
- `deep-ai` 端点加 cli 分支：`provider=cli-claude` → `run_chat_cli_stream`（订阅免 key），API 路径保留。
- 前端 `DeepSkeleton` 允许 cli-claude（无 baseURL 也可触发"交 AI"）。
- 前端侧栏 nav 加"短线候选池"(`/candidates`) + "价值选股漏斗"(`/value-funnel`)。
- 已查明：先前 deep-ai 的 ConnectionReset 是后端用 `&` 前台启动、命令返回后进程被 Windows 回收所致，非代码 bug；改用持久后台任务（run_in_background）后 210s 长流式稳定。

## 合规自查

- ✅ 无买卖方向/参考价位/收益预测/主观评分
- ✅ 护城河只出客观代理（毛利率持续性/行业排名/ROE 稳定性），综合判断交 AI
- ✅ 估值位置只标分位不划买卖线
- ✅ L4 文字交用户 AI（不预设方向）
- ✅ 涨停四池不涉及（中长线路径不触）
- ✅ 用户私有数据不涉及

## 未尽事项（后续）

- `run stage=all` 完整四层 live 压测（已加 TTL 缓存缓解，待大规模实跑验证）
- 港股/美股去劣（本期只 A 股，预留）

## 结论

S005 P1（中长线价值选股漏斗）**实现完成**，AC1-AC10 全部通过（live 或单测/代码审查），合规自查全绿。与短线 S002 并列独立，共享 em_get 限流与 financial_rigor 复算纪律。
