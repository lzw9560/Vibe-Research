# S044 验收说明

> 日期：2026-08-09
> 分支：`feature/S044-funnel-datasources`（off develop，含 e61dd88 S037 fix rebase）
> 级别：large（feature 分支 + grill + 验收）

## 已实现（7 stage，逐 stage commit）

| stage | R | 内容 | 验证 |
|---|---|---|---|
| 3 | R3 | 公告类型化 `classify_announcement` + `_filter_r3 ann_types` 按类型过滤 | mock ✓ |
| 5 | R5/R9 | `_filter_r2` 北向绝对值过滤 + `BaseThreshold.northbound_abs_min` + `IndicatorSet.dragon_tiger_hot_money_relay` 字段 | mock ✓ |
| 1 | R1 | `fetch_northbound` 经 `astock.eastmoney_datacenter`（`RPT_MUTUAL_HOLDSTOCKNDATE_STA` / `HMC_CHANGE`，em_get 防封） | **live 历史验证**（000001 2024-08-16=20916.5万）+ mock ✓ |
| 4 | R4 | `fetch_dt_hot_money_relay` 聚合（`RPT_BILLBOARD_DAILYDETAILSBUY/SELL`，datacenter 防封，不输出个体席位名） | **live 验证**（920176 8次上榜=10154.7万，920117 7次=-3275.7万）+ mock ✓ |
| 2 | R2 | `fetch_sector_flow` **已接线**：push2 clist(fid=f62)+ut + individual_info 行业匹配 | mock ✓（live 待 IP 冷却） |
| 6 | R6 | `_STAGE_MAP` + 条件标注；look-ahead missing 保留（R5 实现）+ `list_for_stage` 可复用 | mock ✓ |
| 7 | R7 | activity 历史走 kline 复算（amount/amplitude/change/vol_ratio + turnover 近似） | mock ✓ |

**测试**：`candidate_funnel/tests/` 127 passed（`-m "not live"`）；**无裸调 requests**（A11 防封合规，全经 `astock.eastmoney_datacenter` em_get 限流 / astock facade）。

## 偏差与待办（需决策）

### 1. 东财 push2/push2his——缺 ut token（根因，非端点宕）+ IP 限流
- **根因发现**：push2/push2his 端点本身可用，但需 `ut` token；astock 旧函数（`stock_fund_flow_120d`/`concept_blocks`）缺 ut → 返空/断连（此前误判为"端点宕"）。
- 验证：push2 clist + ut → 200+真实板块资金流（电子 f62=267亿…）；push2his fflow + ut → 200+klines；push2 slist + ut → data:null（concept_blocks 需另行修复，非仅 ut）。
- **IP 限流**：本会话探测过多触发东财 IP 限流（冷却 60s 仍 RemoteDisconnected），live 验证短期受阻，需数小时冷却。
- **bonus 修复（未做，data 层 scope 外）**：`stock_fund_flow_120d` 加 ut 即修 main_net（app-wide，28 消费者）；`concept_blocks` 需更多（slist data:null）。

### 2. R1 北向——2024-08-19 规则变更后个股日级北向停更
- `RPT_MUTUAL_HOLDSTOCKNDATE_STA` 数据仅到 2024-08-16（规则变更日）。
- 近期日期（post-change，漏斗 T-1 实际用途）→ `fetch_northbound` 返 None=missing（spec §9 回退，不臆造）。
- 历史 backfill（pre-change）有数据（live 验证 20916.5万）。
- **影响**：R1 对漏斗当前用途实际值≈missing；仅历史回填有用。`NorthFlowSegmenter.RULE_CHANGE_DATE` 已预见。

### 3. R2 板块资金流——已接线（push2 clist + ut + individual_info）
- **已接线**：`fetch_sector_flow` 经 `astock.em_get` push2 clist(fid=f62, ut=_EM_PUSH2_UT) 取行业板块主力净流入 + `astock.individual_info` 取个股行业，行业名匹配 f14 → f62(元)→万元。mock ✓。
- push2 slist（concept_blocks）带 ut 仍 data:null——concept_blocks 需另行修复（非仅 ut）；不影响 R2（R2 用 clist 行业板块，不用 slist）。
- **R4 live 已验证**（datacenter，920176=10154.7万，见 live smoke）；R2 live 待 push2 IP 限流冷却后补。

### 4. R4 龙虎榜游资接力——live 多日接力未验证
- `fetch_dt_hot_money_relay` 走 datacenter（`RPT_BILLBOARD_DAILYDETAILSBUY/SELL`，防封，可达）。
- relay 聚合逻辑（>=2 日出现的席位净额合计）mock 测覆盖；**live 验证**：920176（8次上榜）=10154.7万、920117（7次）=-3275.7万（datacenter 真实游资接力聚合）。
- 非上榜股返 None 正确（000001/600519 未上榜 → None）。

### 5. R7 turnover 近似
- 历史 `turnover_pct` 用 `tencent_quote` 当日 `float_market_cap`/`price` 作流通股本近似（`individual_info` 走 push2 宕）。
- 短期近似（lockup 日偏差），缺失标 missing；非臆造（disclosed 基础）。

## live smoke（spec 8b-d）
- **R1 历史 ✓**（000001 2024-08-16=20916.5万，datacenter）；R1 近期 None（2024-08-19 规则变更停更）。
- **R4 ✓**（920176 8次上榜=10154.7万，920117 7次=-3275.7万，datacenter 真实游资接力聚合）。
- R2 接线正确（push2 clist+ut，限流前验证 200+真实板块资金流）；live 待 push2 IP 限流冷却（本会话探测触发）后补。
- datacenter 端点健康（R1/R4 走 datacenter-web，未限流）；push2/push2his 限流（缺 ut + IP 限流双重）。

## 回滚
- 各 stage 独立 commit（feature/S044），`git revert` 即回滚。
- R2 防御式（默认 None=不过滤）；R5 `northbound_abs_min` 默认 0（不过滤）——向后兼容。
