# 核心（轻量、秒装）——行情 + 研报即刻可用
fastapi>=0.110
uvicorn[standard]>=0.29
requests
python-dotenv>=1.0

# 飞书 App Bot 模式（可选，用于更稳定的消息推送）
lark-oapi>=1.0

# 全栈数据源（较重）——一致预期 / 新闻 / 公告 / K线 / 财务
# 未安装时对应端点会返回 501 + 安装提示，不影响行情/研报
akshare>=1.10
mootdx>=0.10
pandas

# S017/S018 预测模型栈 + 特征工程（可选，未装则 predict/ 降级或 501）
# 版本为 Windows venv 实测可用（lightgbm 4.7 Windows 训练有 C++ access
# violation 风险，selection 默认走 RandomForest；shap/borutapy 为 selection
# 增强，缺失时降级 permutation_importance）
scikit-learn>=1.7
lightgbm>=4.3
catboost>=1.2
hmmlearn>=0.3
mapie>=1.4
shap>=0.49
boruta>=0.4

