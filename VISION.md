# Vision

## 一句话

给你一套投研的**框架和工具**，结论由你自己的 AI 给、由你自己负责。

## 这个项目不做什么

市面上的 AI 投研工具，无论包装成什么样，最后几乎都会给你一个答案：买还是不买、目标价多少、评几分。
开源的多 agent 框架也一样——TradingAgents 的流程末端是 `trader.py`，ai-hedge-fund 是
`portfolio_manager.py`，十几个 agent 讨论半天，出口是一个操作指令。

Vibe-Research 不给这个答案。不是因为做不出来，是因为**给答案的那一刻，责任就转移了**。

一个开源项目没有资格替你承担这个责任，也没有能力对你的持仓负责。更实际的是：
你的风险承受能力、持仓结构、资金周期、认知边界，这个工具一样都不知道。
在这种情况下给出「建议」，本质上是把一段看起来专业的文本包装成决策——这对使用的人不是帮助。

所以产品边界是清楚的：

| 做 | 不做 |
|---|---|
| 把客观数据拉全、摆清楚 | 推荐买什么 |
| 提供分析框架（五维、多空、反思） | 预测涨跌与价位 |
| 呈现公开榜单的事实 | 给买卖时机、目标价、评级打分 |
| 指出分歧在哪、还缺什么证据 | 替你下判断 |

## 为什么是「多视角」而不是「一个答案」

多空辩论这个功能最初的问题是：一个 AI 分析个股，说得头头是道，你怎么知道它是对的？

答案是你不知道。但如果让两个 agent 拿着**同一份客观数据**、一个尽力立论一个尽力质疑，
你会立刻看到三件事：

1. 哪些是双方都认的**事实**；
2. 哪些是同一个数字被解读成了两个方向（**分歧的根源是解读**）；
3. 哪些是双方都拿不出数据、只能存疑的（**分歧的根源是缺数据**）。

这三件事比任何一个「结论」都有用。因为它把「你该信谁」变成了「你接下来该去查什么」。

中立主持的输出刻意以**验证清单**收尾，而不是以判断收尾：每条分歧写清楚
「要判定它，需要看什么数据、去哪看、什么时候能看到」。

## 反思：投研里最贵的错误

不是算错数字，是**把推测当成了事实**，并在此之上继续推理。

一段分析读起来越顺，越容易滑过去。所以有了「反思审计」：不产出新观点，
只回头审一遍已有的分析——哪些话有数据撑着、哪些是脑补、如果最终错了最可能错在哪一环。

它的输出同样不是结论，是**怎么继续验证**。

## 数据的立场

- **能拿到真数据就绝不让模型编**。所有工具返回的都是公开接口的实时数据，
  拿不到就明确写「该数据缺失」，并要求模型不得臆测。
- **数据源分层、优雅降级**。主源挂了走备用源，备用源也挂了就如实报缺，
  不用「看起来合理」的东西填空。
- **限流是要尊重的**。走节流接口的抓取保持串行，宁可慢几秒也不冒被封的风险——
  被封的是使用者的 IP，不是我们的。

## 你的数据是你的

持仓、自选股、上传的研报、API key，全部只存在你自己机器上（`~/.vibe-research/`），
不上传、不进仓库、不经过任何第三方服务。模型用的是你自己的 key 或你本机已登录的 CLI，
后端只做转发、不持久化任何配置。

## 一句话收尾

**框架是公开的，数据是客观的，结论是你自己的。**
