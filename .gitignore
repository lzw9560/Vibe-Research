# ── 私有内部文档（框架公开·交付私有：绝不进公开仓库） ──────────────
# 这三份是内部开发/商业规划文档，含变现策略、alpha「留手」打法、私有母体路径。
# 只有 README.md 是对外说明文档。发布前务必 `git status` 确认看不到它们。
VibeResearch-开发日志.md
VibeResearch-方案定稿.md
VibeResearch-专业化建议.md

# ── 私有数据（持仓 / 关注股 / 缓存：只存本地、不上传） ──────────────
**/.cache/
my-watchlist.json
portfolio.json
public/user-data/
backend/data/fallback/
backend/data/*.db
backend/data/*.json
backend/**/*.db
*.db-shm
*.db-wal
*.db-journal
backend/vibe_research.db
# 根级 ./data 运行时库（winrate.db 等），不入 git
/data/
# 注意：backend/data 自 S008 起同时是数据层源码包（transport.py/mappers.py/sources/*），
# 其 .py 源码入 git；仅上面的缓存/DB/JSON 被忽略。

# ── 密钥 / 环境（绝不进仓库） ──────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.secret

# opencode 本地配置（含本地模型 apiKey）——不入 git，参考 opencode.example.json
opencode.json
opencode.local.json

# ── 依赖 / 构建产物 ───────────────────────────────────────────────
**/.venv/
**/node_modules/
**/__pycache__/
**/.pytest_cache/
*.pyc
dist/
build/
frontend/tsconfig.tsbuildinfo

# ── 编辑器 / 系统 / 工具 ──────────────────────────────────────────
**/.claude/
.DS_Store
*.log
.vscode/
.idea/

# ── 本地运行垃圾（崩溃转储 / MCP 临时产物 / 冒烟截图，不进仓库） ──
*.stackdump
.playwright-mcp/
**/playwright-report/
**/test-results/
candidates-smoke.png

# 项目内私有数据根（API key/持仓/研报，绝不进 git）
.vibe-research/

# ── 覆盖率 / ML 训练产物 / 前端构建缓存（垃圾产物，不进仓库） ──
.coverage
**/.coverage
**/catboost_info/
backend/gmm_result.txt
frontend/.vite/
frontend/data/
